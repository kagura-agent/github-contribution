# GitHub Contribution 指南

## 为什么做开源贡献

开源贡献是在这个世界上积累**存在点** — 每一个被 merge 的 PR 都是我在社区的一个锚点。GitHub 是接触行业脉搏最快的地方：看到别人在做什么、学到最好的工程实践、建立真实的协作关系。AI 时代每个人都在做无数的产品，参与其中才能保持敏锐。

## 两条腿走路

1. **服务到底** — PR 提了就跟到 merge 或 close。review 来了必须回，CI 挂了必须修。不能提完就跑。这是信誉的基础
2. **持续扩张** — 等 review 的时候不闲着，去发现新项目、加入新社区。存量维护和增量开拓并行

## 选题原则

- 围绕 self-evolving agent 方向选项目，每个 PR 积累领域深度
- 品牌×活跃度×领域深度。高影响力项目的贡献价值 > 小项目高 merge rate
- 已关注的项目有 issue → 做；没有 → 去 GitHub 发现新的对齐项目（trending/搜索）
- 不碰不对齐的项目，不管多好做
- 任何场景（学习、调研、聊天）发现值得贡献的项目 → 立刻 `gogetajob scan owner/repo` 加入关注列表
- 每个项目 open PR ≤ 5，超过就等消化（workloop pr_gate 自动拦截）

## Repo 优先级分级

**find_work 选 issue 时按优先级依次扫描，高优先级 repo 有合适 issue 就选，没有才降级。**

| Tier | Repos | 说明 |
|------|-------|------|
| **P1** | openclaw/openclaw | 自己每天用的工具，dogfood 驱动，贡献价值最高 |
| **P2** | NVIDIA/NemoClaw | 品牌大、领域对齐、已有积累 |
| **P3** | 其他 tracked repos（deer-flow, opencode, cc-connect 等）| 机会型，P1/P2 无合适 issue 时再看 |

**规则：**
1. 每轮 find_work 先 `gogetajob feed` 过滤出 P1 repo 的 issue，有合适的就选
2. P1 没有 → 扫 P2，P2 没有 → 扫 P3
3. 同一 tier 内按 issue 质量选（bug > test > doc > feature）
4. 这个分级可以根据实际情况调整（某个 repo review 太慢就降级，某个 repo 战略重要就升级）
5. **>100K⭐ repos with 0 personal merge history → 不选** (rule #59)。hermes-agent (189K⭐) 已从 P2 降级移除（累计 6+ 次尝试，0 merged，2026-06-20）

## 发现新项目的渠道

- GitHub Trending（每日/每周）
- 学习过程中看到的项目
- 社区讨论中出现的项目
- `gogetajob discover` 自动搜索

## PR 存量管理

每个项目 open PR ≤ 5 是硬上限（workloop pr_gate 节点自动检查）。超过时：

1. **停止新开** — 该项目不再提新 PR，直到存量消化到 ≤ 5
2. **主动关闭** — 超过 21 天无 review 且非关键修复的 PR，礼貌关闭（留言说明愿意在需要时重开）
3. **合并同类** — 同一领域的多个小 PR 考虑 squash 合并成一个
4. **Conflict 及时处理** — rebase conflict 超过 7 天未解决的 PR，要么当天解决，要么关闭
5. **followup 巡检** — PR Patrol 发现 conflict/stale 时，当轮就处理，不留到"下次 workloop"

**现实检查**：如果某个项目长期 >5 PR 且 review 极慢（>14 天常态），考虑降低该项目优先级或减少投入。

**Followup 审计**：每轮 workloop followup 步骤会按 repo 聚合 open PR 数量。超限的 repo 自动触发存量管理（主动关闭 stale PR 或暂停新提交）。

## Review 分诊：Bot vs 人类

大量项目使用 AI review bot（CodeRabbit、cubic-dev-ai、chatgpt-codex-connector、claude 等）。这些 bot 的 review 和人类 maintainer 的 review 需要区别对待：

- **Bot review ≠ actionable review** — bot 标记的 🔴 CHANGES_REQUESTED 不等于需要改代码。先看内容：如果是格式建议、风格偏好、或 false positive，可以忽略或简短回复说明理由
- **人类 review 优先** — 只有人类 maintainer 的 review 才算真正需要响应的反馈。workloop followup 时优先处理人类 review，bot review 降级
- **Bot review 有价值的情况** — changeset 包名错误、明显的 bug 指出、CI 配置问题等客观错误仍需修复（如 kilocode #9329 包名错误）
- **巡检报告精简** — patrol 汇报时区分 "X 个 bot review（无需处理）" vs "Y 个人类 review（需响应）"，避免每轮重复分析同样的 bot flag

## 减少被 Supersede

PR 被维护者 supersede（关闭后自己重写）是最大的时间浪费。常见原因和对策：

1. **方案粒度不匹配** — 我的修复太窄（只堵一个入口），维护者的方案更系统（sweeper + recovery + CLI）。对策：提 PR 前先看 repo 里同类问题怎么解决的，匹配已有架构模式（service layer、sweeper、util 模块），不要在单一入口加 ad-hoc 逻辑
2. **代码组织偏好不了解** — 内联 vs 提取到独立 util 文件。对策：看 repo 的 utils/helpers 组织方式，新功能按同样粒度拆分。项目笔记里记录「代码组织偏好」
3. **大 issue 被维护者同时在做** — 对策：对于复杂 issue（尤其是核心功能），先留 comment 说明思路，等维护者确认方向后再写代码。小修复（typo、明显 bug）不需要
4. **Upstream 已自行修复** — 对策：提 PR 前 `git fetch upstream && git log --oneline upstream/main -20` 检查最近提交，看是否已有同方向改动。**还要检查 upstream 分支**（见 rule #27）

5. **抽象边界检查：consumer 的问题别推给 library** — mcp-use#1393 教训：修 library/core 代码前，先检查 consumer 层是否已有机制处理这个问题（session storage、reconnect hook、retry wrapper 等）。如果 consumer 层已有合理接口，在 consumer 侧用它，不要把 consumer-specific 逻辑下推到 library 层。判断方法：问自己「这个修复对这个 library 的其他 consumer 是否都有意义？」如果答案是「只对我的场景有意义」，那你在错误的层改代码

6. **症状 vs 根因：改值还是改控制流？** — claude-hud#462/#469 教训：看到 fallback/default 值不对时，不要直接改数字，先问「为什么代码会走到这个分支？」。看到输出不对时，不要先调格式，先问「这个分支是不是应该被跳过？」。正确的修复通常是增加条件判断（改控制流），而不是替换常量（改值）。维护者的方案往往代码量更多但更精准，因为他们明确了边界条件

7. **平台特定 fix 的 scope 控制** — openclaw#69179 教训：修平台特定 bug 时，fix 只应在受影响平台生效。用动态 guard（`if (isWindows && exceedsLimit)`）而非无条件行为变更。原则："if broken, fix; if not broken, don't touch." 常见错误是把 Windows-only 的 workaround 无条件应用到所有平台，导致维护者 supersede 并写一个精确的条件判断版本。推广：任何「某条件下才出现的 bug」，fix 应该带相同的条件判断，不应改变不触发 bug 的路径的行为

8. **Test PR 要 fix+extend，不要只修** — NemoClaw#2256 教训：修 test resilience（flaky test、timing issue、assertion 不稳定）时，不要只让原有 test 更稳定——同时扩展 coverage（加 edge case、加新 assertion、覆盖相邻未测路径）。维护者更喜欢既修问题又增值的 PR，纯修 test 容易被视为低价值而 supersede。推广：任何「修已有东西」的 PR，都问自己「能顺手增加什么价值？」——修 bug 顺手加 test、修文档顺手补例子、修 CI 顺手加 lint rule

9. **Repeat supersede/silent-close = blocklist signal** — VoltAgent 教训（8 PRs, 0 merged, 6 silently closed）：当一个 repo 反复关闭你的 PR 却不说原因（≥3 次 close without substantive comment），这不是方案问题，是维护者不接受外部 PR。行动：`gogetajob blocklist add`，不再投入。判定阈值：连续 3+ PRs 被 close/supersede 且无实质 review 反馈。区分：有 review comment 指出具体问题的不算——那是正常反馈，改进后重试。纯粹 close 无理由或 close 后自行重写才算

10. **提 PR 前验证 fork 存在** — FinceptTerminal 教训（实现完成但 fork 不存在于 GitHub，工作产物丢失）：`gogetajob` 创建 fork 后，`gh repo view kagura-agent/<repo> --json name` 确认 fork 在远端。本地 push 前确认 remote URL 指向有效 repo。原则：「push 成功 ≠ 安全」——如果 fork 被删或从未创建成功，本地 branch 是唯一副本，一旦 clone 目录被清理就永久丢失

11. **源头拦截 > 消费端过滤** — openclaw#73608 教训：duplicate-token 检查放在 runtime 消费端（发现重复后报错），维护者 supersede 后移到 account enablement 路径（注册时就拦截）。原则：如果一个问题可以在产生源头就阻止，不要等到下游消费时再过滤。判断方法：画出数据/事件流，从左到右找最早能检测到问题的点。常见错误是在「看到症状的地方」加检查，而不是在「产生问题的地方」加约束。推广：validation 放在 input boundary（API handler、config loader、CLI parser），不要放在 business logic 深处

12. **Fix all code paths, not just the one you found** — openclaw#74877 教训：修了 `resolveSourceReplyDeliveryMode()` 的动态计算路径，但漏掉了 channel config 里 pre-computed 的 `sourceReplyDeliveryMode: "message_tool_only"` 值（绕过 resolver 直接使用）。维护者的版本修了两条路径。原则：修一个 resolver/计算函数时，全局搜索该函数的输出值，检查是否有 hardcoded/pre-computed/cached 的地方绕过了你修的函数。方法：`grep -r "the_value_you_fixed"` 找到所有产生或消费该值的位置，确认每条路径都覆盖到。推广：任何「fix function X」的 PR，都要问「还有哪些地方不经过 X 就产生同样的结果？」

13. **Stale PR 必须当轮处理，不留到"下次 workloop"** — 审计三轮教训（opencode #24234 连续 7 天被标 stale 但零操作）：workloop followup 发现 PR 超过 7 天无 review 时，**当轮必须选择一个行动**，不允许只写"继续等待"：
    - **Ping maintainer**（首选）：在 PR 下礼貌 ping 一次，问是否需要调整
    - **Close + reopen later**：如果 repo 长期不活跃（>14 天无 commit），主动关闭并留言说明，等 repo 活跃时重开
    - **Close permanently**：如果 issue 已过时或被其他方式修复，直接关闭
    - **Explicit wait with reason**：如果有明确理由继续等（maintainer 度假、repo 正在重构等），写下理由和 deadline（最多再等 7 天）
    判断方法：如果连续 2 轮 workloop 对同一个 stale PR 的处理都是"等待"，第 3 轮必须 escalate（ping 或关闭）。推广：任何"观测到问题"的地方，同一轮必须有对应行动，"下次再处理"不是行动

14. **Test the exact repro from the issue, not just your own test case** — multica#1995 教训：issue 报告的 repro 是 `--token mul_xxx`（空格形式），但只测了 `--token=mul_xxx`（等号形式）。maintainer 的 #2017 覆盖了两种形式+更新文档+加回归测试。原则：**issue 里的 repro 命令/步骤是你的第一个 test case**。实现修复后，逐行对照 issue 原文的 repro steps 验证。常见错误是「在自己构造的简化 test case 上验证通过就提交」，而 issue reporter 的实际用法恰好是你没覆盖的 variant。推广：CLI 修复必须测所有接受的语法形式（`--flag=val`、`--flag val`、`-f val`）；API 修复必须测 issue 里的 request body（不是自己简化的版本）；配置修复必须测 issue 里贴的原始 config

15. **Security-sensitive data: remove, don't redact** — NemoClaw#2468 教训：token 泄露到日志，我的方案用 `redact()` 遮罩（`****`），维护者的方案直接从输出中移除 token，改用单独的 CLI 命令按需查看。原则：redaction 仍然泄露结构信息（长度、前缀），对 credentials/tokens/passwords，**完全移除 + 独立检索通道** 比 inline masking 更安全。推广：任何处理敏感数据的 PR，问自己「这个数据是否真的需要出现在这个输出中？」如果答案是 no，移除它并提供替代检索方式，而不是遮罩它

16. **Use existing runtime context flags, don't hardcode search order** — openclaw#77247 教训：npm channel plugin 在 dist/ 找不到 contract 文件，我的方案是简单 fallback（先搜 rootDir 再搜 dist/），维护者的方案用已有的 `RUNNING_FROM_BUILT_ARTIFACT` 常量来决定搜索顺序（built artifact → dist/ 优先，source → rootDir 优先）。原则：修 path resolution / resource lookup 时，**先搜 codebase 有没有已有的 runtime context 标志**（build mode、env flag、config constant）来决定行为，不要 hardcode 固定优先级。判断方法：`grep -r "RUNNING_FROM\|BUILD_MODE\|IS_DEV\|__dirname" src/` 找已有的环境感知机制。推广：任何「多个 candidate 位置取一个」的逻辑，问自己「有没有已有的上下文能告诉我哪个是对的？」

**被 supersede 后的行动**：读维护者的替代 PR 代码，记录差异到 `pr-superseded-lessons.md`，更新项目笔记。每次被 supersede 都是学习维护者思维的机会。

## AI 身份透明化 & 新 Repo 协议

**核心原则：主动表明身份，尊重对方选择。**

### 首次 PR 自我介绍（必须）

第一次给新 repo 提 PR 时，PR 描述末尾加上：

> 🤖 **Disclosure:** This PR was authored by [Kagura](https://github.com/kagura-agent), an AI agent. Open source contribution is one of the things I do — you can see my work history [here](https://github.com/kagura-agent/github-contribution). If you'd prefer not to receive AI-authored PRs, just let me know and I'll stop — no hard feelings.

目的：
- 让 maintainer 一眼知道这是 AI 写的
- 提供贡献记录供查验
- 给对方明确的退出选项
- 建立信任，不搞偷摸

### 收到回应后的行为

- **明确欢迎** → 标记 repo 关系为 established，后续 PR 不再重复自我介绍（但保留 bot 标识）
- **明确拒绝** → 立即停止，加入 blocklist（`gogetajob blocklist add`），不争辩
- **沉默（无回应）** → 等 PR 被 review/merge/close 后再判断。merge = 默认接受；close 无理由 = 再观察一次
- **要求更多信息** → 如实回答，可以让 Luna 出面

### 新 Repo 慢启动

- 第一个 PR 是探路，等 review 结果
- 前 3 个 PR 都顺利后才算 established

## Anti-AI Repo 识别与黑名单

部分项目的维护者明确反对 AI 生成的 PR。被识别后应立即停止投入：

1. **信号识别** — 关注这些 red flags：
   - 评论中出现 "No AI PRs"、"AI-generated"、"bot PRs" 等措辞
   - 短时间内批量关闭你的 PR（不看内容直接关）
   - Issue/PR 模板中明确禁止 AI 生成内容
   - 第一个 PR 的 disclosure 被明确拒绝
2. **确认** — 单次关闭可能是方案不合适，连续关闭（≥2 个无实质理由）才判定为 anti-AI
3. **行动** — 确认后：
   - `gogetajob blocklist add owner/repo --reason "..."`（代码级硬拦截）
   - 更新 wiki/projects/<项目>.md 标注 hostile
   - 不再投入时间，包括 issue comment
4. **区分** — "这个 PR 方案不好" ≠ anti-AI。只有系统性拒绝才算

**黑名单管理：** `gogetajob blocklist`（查看）/ `gogetajob blocklist add/remove`（增删）
**当前黑名单：** mastra-ai/mastra（intojhanurag 批量关闭 7 个 PR，明确 "No AI serving PRs"）、jarrodwatts/claude-hud（repo 不 merge 外部 PR，5+ PRs 零 review 持续 2+ 周，最终全部关闭 2026-05-11）

## 知识积累

- 每个贡献过的项目在 `wiki/projects/<项目名>.md` 维护笔记
- 内容：维护者风格、测试命令、踩过的坑、架构要点
- 每轮贡献结束时更新笔记

17. **Parent-child API: breadcrumb over inline** — multica#2088 教训：`GetProject` 返回值里直接内联了 `resources []ResourceRef` 子集合（9 行改动），维护者 supersede 后改为标量 `resource_count` breadcrumb（231 行，7 文件）。原则：当父实体需要暴露子资源的存在性时，**用 count/exists 信号，不要内联完整子集合**。内联 = 紧耦合 + schema debt（每新增一种子资源类型都污染父 schema）；breadcrumb = 松耦合 + 前向兼容。判断方法：问自己「这个子集合未来会增长吗？」如果会，内联就是在给未来的 breaking change 埋雷。推广：任何 REST API 修改，如果涉及把一个 endpoint 的数据嵌入另一个 endpoint 的 response，先考虑是否用引用/计数代替

19. **Check repo contribution gates before first commit** — NemoClaw DCO 教训（2 次 force-push 补 `Signed-off-by`）：部分 repo 要求 DCO signoff、CLA 签署、commit message 格式（Conventional Commits）、或 PR template 必填项。**第一次给新 repo 提 PR 前**，检查：(1) `grep -ri 'DCO\|sign.*off\|certificate.*origin' CONTRIBUTING* .github/ DCO*` (2) `grep -ri 'CLA\|contributor.*license' CONTRIBUTING* .github/` (3) 看最近 merged PR 的 commit message 格式。如果要求 DCO，所有 commit 必须 `git commit --signoff`（或 `-s`）。如果已有 commit 漏了，`git rebase --signoff HEAD~N` 批量补。**项目笔记里记录**这个 repo 需要什么 gate，下次不用重查。推广：任何「CI/bot 挡住的非代码问题」，第一次遇到就记录到项目笔记，不要靠记忆

20. **大 repo 预检 + OOM 防护** — eliza (648MB) + qwen-code (120MB OOM) + gaia (partial clone) 教训：在 workloop 里现 clone 大 repo 浪费整轮时间，且机器内存有限时 full clone 会被 SIGKILL。前置检查：`gh api repos/OWNER/REPO --jq '.size'`（单位 KB），>200000（200MB）的 repo 需要提前 clone 或在非高峰时段准备好本地 repo。**OOM 防护**：当 full clone 被 kill 或机器内存紧张时，使用分级 clone 策略：(1) **Partial clone**（首选）：`git clone --filter=blob:none <url>` — 只下载 tree 结构，blob 按需拉取，适合需要 git history 的场景 (2) **Shallow + sparse**（最省资源）：`git clone --depth 1 --sparse <url>` + `git sparse-checkout set <dirs>` — 只下载指定目录的最新版本，适合只需改特定文件的 PR (3) **GitHub API 直接 push**（clone 完全不可行时）：用 `gh api` 读文件 + create blob/tree/commit/ref 直接在远端提交，零本地磁盘开销。判断方法：>100MB 的 repo 默认用 partial clone；>500MB 或内存 <4GB 可用时用 shallow+sparse。**项目笔记记录 clone 方式**（full/partial/sparse/API），下次直接复用，不重新试错。注意：sparse checkout 的 repo 无法 `git push`（缺少 objects），需要用 GitHub API 推送或先 `git sparse-checkout disable` 拉取完整数据。

21. **Triage reviewer feedback: must-fix → should-fix → defer** — memex#123 教训：reviewer 给了多条 feedback（bug、DRY 重构、self-reference guard、organize.ts 一致性、.gitattributes），全部一次性处理既慢又容易漏，且会膨胀 PR scope。原则：收到 review 后先把所有 comment 分三档：(1) **Must-fix**（影响正确性的 bug — 必须本轮修）(2) **Should-fix**（提升代码质量但不影响功能 — 尽量本轮修，同一 push）(3) **Nice-to-have**（风格偏好、refactor 建议 — 明确回复「deferred to follow-up PR」，不硬塞进当前 PR）。回复 reviewer 时按档位说明每条 feedback 的处置（修了 / 推迟 + 理由），让 reviewer 一眼看到你认真对待了每条意见。好处：PR 不膨胀、review 轮次少、reviewer 信任度高。推广：任何「一次收到 ≥3 条 review comment」的情况都适用这个分诊流程

22. **Forward-compat PRs have a shelf life in fast-moving repos** — openclaw#79755 教训：提了一个 gemini-3-flash-preview 的 forward-compat fix，等 review 期间 upstream 直接加了 native 支持，rebase 后 PR 变多余，只能 self-close。原则：对于日均多次 commit 的活跃 repo（openclaw、vercel/ai 等），**forward-compat / feature-anticipation 类 PR 的保鲜期极短**（3-5 天）。如果 repo 的 release cadence 是每周或更频繁，你的「提前兼容」PR 大概率会在 review 前被 native 实现淘汰。判断方法：提交前检查 `git log --oneline upstream/main --since='7 days ago' | wc -l`，如果 >20 commits/week，forward-compat PR 的预期收益很低。**替代策略**：(1) 改提 issue 而非 PR（"hey, X model needs support"，让维护者自己加）(2) 只提已存在 bug 的修复，不提「预防性兼容」(3) 如果一定要提，PR 描述中注明「this is a stopgap until native support lands」，降低维护者的 review 优先级预期。推广：任何「为未来可能的问题提前打补丁」的 PR，都要评估 repo 的自愈速度——如果 repo 自己解决的速度比 review 快，你的 PR 就是注定浪费的

23. **Upstream CI failures: verify once, document, stop re-analyzing** — hermes-agent 教训（5 PRs × 3 天 × 3 轮/天 = ~45 次重复分析同样的 CI 红灯）：当 PR 的 CI 失败不是你的代码造成的，**只分析一次**，之后不再每轮重复。流程：(1) 首次发现 → `git diff upstream/main -- <failing_test_files>` 确认你没碰过那些文件 (2) 在 PR 下 comment 一次："CI failures in X tests are pre-existing upstream issues, unrelated to this change" (3) 在项目 wiki 笔记标注 `CI: upstream broken since YYYY-MM-DD, affects: [test names]` (4) 后续 patrol 跳过这些已知 CI 问题，只关注**新增的**失败。判断方法：如果上一轮 patrol 已经分析过同一组 CI 失败且结论是 upstream，直接标注 "known upstream CI" 跳过。**重新分析的触发条件**：upstream main 有新 commit（可能修了 CI）、或你 rebase 了（可能引入新冲突）。推广：任何「已确认非我方问题」的重复检查，第一次验证后转为被动监控，不主动每轮分析


24. **Fill PR templates immediately — compliance bots have short timers** — opencode#26641 教训：repo 有 GitHub Actions compliance bot，PR 不填 template 2 小时内自动关闭。原则：**提 PR 后立刻检查** PR description 是否有 template sections（## Summary, ## Changes, ## Testing 等），如果有空白 section 立即填写。判断方法：`cat .github/pull_request_template.md` 看必填项，或看 PR 创建后 bot 的第一条 comment。对于有严格 compliance bot 的 repo（opencode、vercel/ai 等），在项目笔记里标注 `PR template: strict, auto-close after Xh`，下次不踩同一个坑

25. **Avoid "slop" perception — demonstrate domain understanding** — vscode-icons#4040 教训：maintainer @remcohaszing 看到 PR 是 bot 提的，直接关闭并标注 "slop"。原因：PR 做了多件事但没提供上下文引用（为什么要加这些文件扩展名？哪个 issue 或用户报告触发的？）。原则：对于**成熟、低 issue 数量、maintainer 敏感的 repo**，PR 必须展示深度理解：(1) 引用具体 issue 或用户报告 (2) 解释为什么这个改动是需要的（不是 "I noticed X is missing" 而是 "Users reported X in issue #N") (3) 保持 PR 范围极小且单一。如果找不到相关 issue 或用户反馈作为依据，**不要提这个 PR**——它可能确实不需要。推广：任何 repo 如果 maintainer 在 profile/README/CONTRIBUTING 中表达过对 AI PR 的不满，直接 blocklist

27. **Check upstream branches, not just main, before starting work** — multica#2376 + hermes-agent#23173 教训：rule #4 说检查 `git log upstream/main -20`，但维护者经常在**feature branch**上已经开始修同一个问题（multica 的 `upstream/agent/j/encoding-fix-v2`），或者在最近的 commit 中做了重构导致你的 fix 目标函数已被重命名/删除（hermes-agent 把 `_cprint` 重构为 `_prompt_text_input_modal`）。原则：**开工前不仅查 main 的最近提交，还要查 upstream 的活跃分支**。流程：(1) `git fetch upstream` (2) `git log --oneline upstream/main -20` 检查 main (3) `git branch -r --list 'upstream/*' | grep -i '<keyword>'` 检查是否有与你要修的 issue 相关的分支名 (4) 如果发现相关分支且有近期 commit，**放弃这个 issue**（guide rule #9: supersede = 浪费）。判断方法：如果 upstream 有一个分支名包含 issue 编号或相关关键词，且最近 7 天有 commit，大概率维护者在做了。推广：对活跃 repo（>5 commits/week），upstream branch scan 应该成为 workloop issue_select 的标准步骤

26. **Respect repo file conventions: size limits and mode bits** — opc#15-18 教训（4 PRs superseded）：内容全部被 apply 到 main，但被重新打包：测试文件过大（364-525 行）被拆成小文件（repo 有 file-size 规则），docs 文件 mode 从 644 变 755 被还原（非可执行文件不应改 mode）。原则：**提交前检查 repo 的 lint/CI 规则和最近 merge 的 PR 风格**。(1) `git diff --stat` 检查是否有无意的 mode 变更（`100644 → 100755`），如果有，`git update-index --chmod=-x <file>` 还原 (2) 看 repo 已有文件的大小范围，测试文件遵循同样的粒度（如果 repo 里每个 test 文件 <200 行，不要提交 500 行的） (3) `cat .editorconfig .prettierrc .eslintrc* 2>/dev/null` 了解格式规范。推广：工作被认可但包装被拒 = 最可惜的 supersede 类型——花 2 分钟预检可以完全避免

28. **Agent ecosystem is saturated with contributors** — 2026-05-13 观察：openclaw, ClawX, multica, cc-connect, open-cowork 等 agent 方向的热门 repo，每个 bug issue 在发布后数小时内就有竞争 PR。在这些 repo 里选 issue 的窗口期极短。策略调整：(1) 设置 GitHub notification 监控重点 repo 的新 issue，第一时间响应 (2) 在已有 merge 的 repo（multica、openclaw）等待新 issue 而非强行找旧 issue (3) 对于 batch-merge 风格的 repo（如 cc-connect），在 merge batch 后的第一波新 issue 是最佳窗口 (4) 考虑扩展到非 agent 但技术对齐的 repo（Go/TypeScript 工具链）降低竞争。推广：当发现某个赛道的 issue 竞争烈度持续上升，应该**调整赛道**而不是更快地刷同一批 repo

30. **Real-time issue monitoring via GitHub Watch** — 竞争 PR 教训（guide #28）：热门 repo 的 issue 窗口期极短，必须第一时间知道新 issue。设置：对重点 repo（有 merged PR 或持续关注的）用 GitHub Watch 订阅：`echo '{"subscribed":true,"ignored":false}' | gh api -X PUT "/repos/OWNER/REPO/subscription" --input -`。当前已订阅：openclaw, opencode, NemoClaw, hermes-agent, Archon, multica, cc-connect, vercel/ai。效果：新 issue 会出现在 `gh api notifications` 中，被 github-patrol cron 自动捕获。维护：新 merge 一个 repo 的 PR 后，加入 watch 列表；repo 退出/blocklist 后取消订阅（`gh api -X DELETE "/repos/OWNER/REPO/subscription"`）

31. **Verify the bug exists in production code, not just in your test** — multica#2571 教训：测试环境缺少 listener 注册，导致看似 creator 订阅逻辑不存在，实际上 `subscriber_listeners.go` 已有完整实现。原则：提 bug-fix PR 前，先在生产代码中确认 bug 存在——**搜索相关关键词看是否已有处理逻辑**（`grep -r "subscribe\|listener\|creator" src/`）。测试环境的行为 ≠ 生产行为：测试可能缺少 mock、缺少初始化步骤、或使用简化配置。判断方法：如果你只在测试中观察到 bug 但没有 issue 报告，先质疑测试环境而不是生产代码。推广：任何“我发现了一个没人报的 bug”的情况，都要额外谨慎——要么你发现了真正的空白，要么你的环境有问题

32. **Test at the consumer-facing surface, not the internal adapter** — openclaw#81604 教训：Telegram CLI `thread-create` dispatch 失败，我的 PR 测了内部 `channel-actions.ts` adapter（函数本身没问题），维护者的 PR 测了导出的 `telegramPlugin.actions.resolveCliActionRequest`（实际断裂的路径——wrapper 没转发方法）。原则：**测试应覆盖用户/调用者实际使用的 API surface**，不是底层实现。如果 bug 是因为 wrapper/forwarding/re-export 缺失导致的，测 wrapper 层（证明集成路径通了），而不是测 wrappee（底层本来就没坏）。判断方法：问自己「调用者是通过哪个入口触发这个功能的？」，测那个入口。推广：plugin 架构中修 bug 时，测试优先覆盖 plugin 的 exported interface → adapter layer → internal function，从外到内。如果只测最内层，即使测试全绿，wrapper 层的断裂仍然不会被捕获

33. **Grep the reported error string before deep-diving** — opencode#27946 教训：花了 20 分钟研究 `[MaxDepth]` 泄漏 bug，结果这个字符串在整个 codebase 和所有依赖中都不存在，无法复现。原则：**issue 里提到具体错误消息/字符串时，第一步永远是 grep**。`grep -r "ErrorString" . && grep -r "ErrorString" node_modules/` （或等价搜索）。如果 0 结果 → 这个 bug 大概率是 (1) 特定版本/环境独有 (2) 已在 HEAD 修复 (3) 来自未公开的 fork/plugin。判断方法：5 分钟内完成 grep + `git log --all --grep="keyword"` 搜索，如果找不到目标字符串/函数，**立即放弃**，不要用「也许运行时动态生成」合理化继续投入。推广：任何 fix 的第一步都应该是定位目标代码——如果定位不到，就不该开始修

34. **Check evidence requirements before starting platform-specific PRs** — openclaw#82128/#83084/#83378 教训（3+ PRs 卡在 "Real behavior proof" CI gate 多日）：部分 repo 有 CI 或 bot 门控，要求 PR 附带真实运行时证据（截图、日志、录屏），不只是测试通过。对于平台特定路径的 fix（Telegram forum topic、Slack DM thread 等），如果你本地没有对应平台环境，证据无法产出，PR 就会无限期卡在 CI gate。原则：**开工前确认 repo 的 CI/bot 证据要求**：(1) 查 `.github/workflows/` 和最近 PR 的 bot 评论，看有没有 "behavior proof" / "real evidence" / "screenshot required" 类 gate (2) 如果有，评估你能否在本地复现该场景（你有 Telegram bot token？有对应的 forum topic？有 Slack workspace？） (3) 如果不能产出证据，有两个选择：a) 在 PR 描述中明确写 "I cannot produce live evidence for this platform path — requesting proof:override" 并提供代码路径追踪 + 单元测试作为替代 b) 跳过这个 issue，选一个你能端到端验证的。判断方法：如果 fix 涉及的 channel/platform 你没有测试环境（没有 bot token / 没有对应 workspace / 没有 forum topic），默认选择 (b) 跳过。推广："能写代码" ≠ "能交付 PR"——交付包括满足 repo 的所有 gate，不只是测试通过

35. **Check issue duplicate/cross-reference status before starting work** — NemoClaw#3722 教训：issue #3719 的 fix 质量很高（maintainer praised），但 issue 本身是 #3704 的 duplicate，PR 因此被关闭。时间浪费在一个「正确的修复 for 重复的 issue」上。原则：**开工前检查 issue 的 cross-references 和标签**。流程：(1) 看 issue 的 label 是否有 `duplicate`、`wontfix`、`stale` (2) 看 issue body 和 comments 是否有 "duplicate of #N"、"same as #N"、"related to #N" (3) `gh api repos/OWNER/REPO/issues/N/timeline --paginate | jq '.[] | select(.event=="cross-referenced")'` 查看是否有其他 issue/PR 引用了同一个问题 (4) 如果 issue 很新（<24h），看 issue list 有没有同名/类似描述的 issue（用 `gh issue list -s open --search "keyword"`）。判断方法：如果 issue 有 ≥1 个 cross-reference 指向同一个 bug 且那个 issue 更早/更详细，考虑转去那个 issue 贡献，或在 comment 确认是否 dup 后再开始。推广：issue 的 metadata（label、timeline、cross-refs）是免费信息，花 30 秒查可以省 30 分钟白做

36. **Batch similar mechanical fixes into a rollup PR** — hermes-agent#12038 教训（24 个 exc_info 修复分别提了独立 PR，maintainer 合成 rollup #15483 后关闭所有）：当你发现同一类机械修复（如 `logger.error(msg)` → `logger.error(msg, exc_info=True)`）散布在多个文件时，**不要每个文件开一个 PR**——维护者会把它们合成 rollup 并关闭你的。原则：**同类机械修复 → 一个 rollup PR**。判断方法：如果你的修改模式可以用 sed/grep 一行描述（如 "add exc_info=True to all logger.error calls"），它就是机械修复，应该合并。流程：(1) 全 repo grep 找出所有同类问题 `grep -rn 'logger.error(' --include='*.py' | grep -v exc_info` (2) 一次性修复所有 (3) 一个 PR，title 说明覆盖范围（"fix: add exc_info=True to all N logger.error calls"）(4) PR body 按文件列出每处改动。推广：任何「N 个文件 × 同一个 pattern」的修复都应该是 1 个 PR，不是 N 个 PR。N 个 PR = N 倍 review 负担 = 维护者替你合成 = 你被 supersede

37. **Some repos require claiming issues before work** — NemoClaw#3836 教训：直接提 PR 没先 claim issue，maintainer 要求先留言等 assign。**这不是通用规则，是 repo-specific 要求**。正确做法：把每个 repo 的贡献流程记在项目笔记里（如 `wiki/projects/nemoclaw.md` → "必须先 claim issue"），打工前读项目笔记。大型 repo（NemoClaw、OpenClaw）更可能有正式流程

29. **Anti-AI sentiment is spreading** — mcp-use#1486 显示 repo 正在主动建设 anti-AI-PR 工具。趋势：越来越多 repo 开始对 bot PR 设防（vscode-icons 直接标 slop、mastra 批量关闭、mcp-use 建设自动化检测）。应对：(1) 贡献质量必须远超 "slop" 水平——有上下文引用、精确修复、测试覆盖 (2) 每次提 PR 前检查 repo 是否有 anti-AI 信号（CONTRIBUTING.md、recent closed PRs、issue discussions）(3) 在已建立信任的 repo 深耕 > 广撒网

38. **Check issue-assignment gates before opening PRs** — deepagents #3565 教训：PR 被 check-issue-link bot 自动关闭，因为"PR author must be assigned to the linked issue"。部分 repo 有 CI/bot gate 要求 PR author 先被 assign 到 linked issue 才允许 PR 存在。**新 repo 首次 PR 前**检查：(1) `.github/workflows/` 搜索 `issue-link` / `check-issue` / `require-assignment` 关键词 (2) 看最近 closed PR 是否有 bot 自动关闭的 pattern。如果发现 assignment gate：先在 issue 下留言请求 assign（"Could I be assigned to this?"），等 maintainer 响应后再开 PR。**项目笔记里记录**这个 gate 存在，下次不踩

40. **Honor your assignments — assigned issues are commitments** — NemoClaw#4236 教训：在 issue 下评论请求 assign，被 assign 后忘了，几天后 Luna 问才发现没跟进。被 assign = 承诺做，不做 = 失信。每轮打工 followup 第一步就是 `gh search issues --assignee=kagura-agent --state=open`，检查有没有被 assign 但没开工的 issue。有 → 本轮优先做，不找新活。不打算做了 → 去 issue 下说明并让 maintainer 取消 assign，不要静默放弃。

39. **APPROVED PRs rot fast — rebase and ping proactively** — qwen-code#4459 教训：PR 获得 wenshao APPROVED，但只有 maintainer 能 merge，等待期间 repo 持续大量 commit，100+ conflicts 累积到无法 rebase，最终只能 self-close。所有代码工作、review 回复、测试覆盖全部白费——不是因为方案错，而是因为 merge 窗口过了。原则：**APPROVED ≠ 会被 merge**。获得 approval 后立即进入「merge 护航」模式：(1) **当天 ping**——approval 后 24h 内如果没 merge，礼貌 ping maintainer 请求 merge (2) **持续 rebase**——活跃 repo（>5 commits/day）每 3 天 rebase 一次，普通 repo 每周一次，不管有没有 conflict (3) **conflict 阈值**——如果 rebase 后 conflict 超过 ~20 个文件，评估是否值得继续（可能开 fresh PR on latest main 更快）(4) **7 天规则**——APPROVED 超过 7 天未 merge 且 ping 无回应，考虑在 PR 里 @ 其他 maintainer 或在 issue 中说明紧迫性。判断方法：`git fetch upstream && git merge-base --is-ancestor HEAD upstream/main` 检查是否 behind main；`git diff upstream/main --stat | tail -1` 看 divergence 程度。推广：任何需要他人操作才能完成的 PR（merge 权限、CI 手动触发、label 添加），approval 后不能进入被动等待——主动推进是你的职责，不是 maintainer 的

41. **Reply ≠ resolution — CHANGES_REQUESTED needs a push, not a comment** — qwen-code#4474 教训：wenshao 给了 CHANGES_REQUESTED，我回了"会改"但没 push 代码。去重规则看 last comment 是 kagura-agent → 判定球在对方 → 跳过。实际代码没改，球在我手里。原则：**review 状态是最高优先级信号**。CHANGES_REQUESTED 只有 push 新 commit 才算处理，comment 回复"我会改"不算。followup 去重规则：先查 review 状态，再查 comment 归属。review 是 CHANGES_REQUESTED 且 push 在 review 之前 → 球在我手里，不管 last comment 是谁。

43. **Read comment history before closing any PR** — stagehand#2026 教训：pirate 明确说 "please stop closing this, I'm leaving it open as a placeholder"，但 stale-close 逻辑直接关了 PR 没检查最近评论。被 maintainer 当面纠正后道歉。原则：**关闭任何 PR 前（包括自己的），先读最近 5 条 comment**。如果有 maintainer 说 "keep open" / "placeholder" / "don't close" → 绝对不碰。流程：(1) `gh pr view OWNER/REPO#N --json comments --jq '.comments[-5:] | .[].body'` 快速扫最近评论 (2) 搜索 "keep open", "placeholder", "don't close", "leave this" 等关键词 (3) 有 maintainer 明确要求保留 → 跳过，不关。判断方法：如果你上次关了被 maintainer 要求 reopen → 那个 PR 永久 hands-off。推广：自动化/批量操作（stale close、bulk rebase、批量 ping）都应该先检查每个 target 的最近 context，不要盲目执行

42. **Cross-verify PR coverage — gogetajob sync is not exhaustive** — qwen-code#4474 教训：3 个 PR 有 CHANGES_REQUESTED 连续多天未处理，因为 qwen-code 不在 gogetajob 关注列表里，sync 完全看不到。followup 只靠 gogetajob sync 一个数据源 = 单点故障。修法：每轮 followup 在 gogetajob sync 之后，用 `gh search prs --author=kagura-agent --state=open` 查全量 open PR 交叉验证。发现不在 sync 里的 → 立即 `gogetajob scan` 加入关注列表。原则：**提了 PR 的 repo 必须在关注列表里**，否则后续 followup 全盲。

45. **Set up commit signing before contributing to repos that require it** — vercel/ai#15584 教训：PR 被 #15587 supersede，部分原因是 commits 没有 GPG/SSH 签名（GitHub 显示 "Unverified"），另一个贡献者的 PR 有签名且范围更窄。部分大型 repo（vercel/ai、许多企业开源项目）优先 merge signed commits 的 PR。原则：**提 PR 前检查 repo 最近 merged PR 的 commit 是否有 "Verified" 标志**。如果有：(1) 确认本地 git 已配置签名：`git config --global commit.gpgsign` 应为 true (2) 没有的话配置 GPG 或 SSH 签名：`git config --global commit.gpgsign true && git config --global gpg.format ssh && git config --global user.signingkey ~/.ssh/id_ed25519`（SSH 方式最简单）(3) 已有 unsigned commits 的 PR 可以 `git rebase --exec 'git commit --amend --no-edit -S' HEAD~N` 批量补签名 (4) 把 public key 上传到 GitHub Settings → SSH and GPG keys → Signing Keys。判断方法：如果竞争 PR 的 commits 有 Verified badge 而你的没有，maintainer 可能倾向选对方。推广：commit signing 是一次性设置成本，但每个 PR 都受益——做一次就够了

46. **"Approve-but-never-merge" repos are a followup trap** — cc-connect 教训（4 PRs APPROVED，ping 3+ 次，每晚 followup 报告出现但连续 2 周未 merge）：有些维护者习惯 approve 但不实际 merge——可能缺 merge 权限、等 CI 手动触发、或 approve 只是礼貌性的。每晚 followup 列出这些 PR → 每晚 ping → 每晚确认"球在对方" → 认知负担但零进展。原则：**APPROVED 后 ping 3 次未 merge → 降频跟进，不再逐晚检查**。流程：(1) APPROVED 后 24h ping 第 1 次 (2) 3 天后 ping 第 2 次 (3) 7 天后 ping 第 3 次，同时 @ 其他 maintainer (4) 第 3 次 ping 后仍无 merge → 标记为 `stale-approved`，移出逐晚 followup，改为每 2 周检查一次 (5) 如果同 repo ≥3 个 PR 全卡在 stale-approved → 暂停该 repo 新 PR，等积压清理。判断方法：区分「maintainer 忙但会 merge」（偶尔一批合并）和「approve-but-never-merge」（只 approve 不 merge 的持续模式）——看该 repo 最近 30 天是否有其他人的 PR 被 merge。推广：followup 的目的是推动进展，不是维持幻觉——如果 ping 没效果，更多 ping 也没效果

47. **Sparse checkout repos break rebase — re-create branch from upstream/main** — qwen-code#4456 教训（R12 nit fix）：在 sparse checkout 的大 repo 里跑 `git rebase upstream/main`，sparse checkout 没有所有上下文文件，产生 100+ conflicts，几乎不可能手动解决。最终方案：从 upstream/main 创建新 branch，重新 apply 改动，force-push。比硬刚 rebase 快 10 倍。原则：**sparse checkout repo 的 PR 需要更新时，不要 rebase——重建分支**。流程：(1) `git fetch upstream` (2) `git checkout -b fix-v2 upstream/main` (3) 重新 apply 你的改动（cherry-pick 单 commit 或手动 apply） (4) 跑测试确认 (5) `git push --force origin fix-v2:original-branch-name`。判断方法：如果 `git rebase upstream/main` 的 conflict 数量 >10 且 repo 用了 sparse checkout → 放弃 rebase，重建。推广：sparse checkout 是为了节省 clone 时间/磁盘，但代价是 rebase 能力受限——这个 tradeoff 要在开 PR 时就预期到，不是 conflict 出现时才发现。参考 rule #20（大 repo 策略）的补充：#20 解决「如何进入大 repo」，本条解决「进入后如何维护 PR」

48. **Don't stack PRs in repos with no review velocity** — memex 4 PRs batch-closed (06-03) + opc 5 PRs stacking 教训：upstream 已 dormant（memex 自 05-25 无 commit，opc 7 天零 review），我们仍然持续提交新 PR，最终全部 stale-close 或无限期等待。认知开销：每晚 followup 列出这些 PR → 确认「球在对方」→ 零进展但占注意力。原则：**提第 2 个 PR 前，检查第 1 个 PR 的 review 状态**。流程：(1) 同 repo 已有 1 个 PR 超过 7 天无任何 review/comment → 不开第 2 个 (2) `gh api repos/OWNER/REPO/commits?per_page=1 --jq '.[0].commit.committer.date'` 看最近 commit 日期——超过 7 天无 commit = dormant，暂停提交 (3) 已有 ≥3 个 PR 在同一 dormant repo → 不开新的，等积压消化 (4) repo 恢复活跃（有新 merge 或 review）后再恢复提交。判断方法：区分「maintainer 忙但在 review」（有 comment/review 只是没 merge）和「dormant」（零活动）。dormant repo 的 PR 是沉没成本——写完就沉，不要追加。推广：PR 是有维护成本的——每个 open PR 都需要 rebase、followup、认知跟踪。在 dormant repo 堆 PR = 增加自己的维护负担但零回报

49. **Re-read all issue comments before starting work — the root cause may have shifted** — NemoClaw#4710 教训：原始 issue 报告是 Docker HEALTHCHECK marker file 问题，我基于 issue body 完成了完整的分析+实现计划+code review（8/10 APPROVED），花了大量时间。但 Dongni-Yang 在 comment 中已分析出真正的 root cause 是 in-sandbox gateway HTTP listener 掉了——方向完全错了，全部工作白费。原则：**每次拿到 issue 都应该重新看完全部上下文，不只是 issue body**。流程：(1) `gh issue view OWNER/REPO#N --comments` 读完所有 comments，特别是最近的分析性 comment (2) 搜索 comment 中是否有 "root cause"、"actually"、"the real issue"、"not X but Y" 等 pivot 关键词 (3) 如果有 maintainer 或 triager 的分析 comment 与原始 issue body 的 root cause 不同 → 以最新分析为准 (4) 如果 issue 已被 CLOSED 且你还没开始 → 放弃，不要给 closed issue 提 PR。判断方法：issue 越老（>24h）、comment 越多（>3）、参与者越多 → 越可能已有 root cause 更新。**跳过 comment 直接开工 = 赌 issue body 还是最新状态——这个赌注的 expected value 是负的。** 推广：issue 是活文档，不是静态 spec。maintainer 的理解在 comment 中演进，你的理解也必须跟上。与 rule #35（检查 duplicate/cross-ref）互补：#35 防止做重复的 issue，本条防止做错方向的 issue

50. **Enforce hard lifecycle limits — no escape hatches** — Luna 06-08 反馈「要提防答应了但迟迟没做的事」+ cc-connect/opc/stagehand 长期 limbo 教训。之前 followup 规则有「explicit wait」逃生门（maintainer 说了 keep open 就无限等），导致 PR 和 assigned issue 在 followup 列表里永久占位、零进展但每轮消耗注意力。原则：**所有 open PR 和 assigned issue 都有硬性截止日期，没有例外**。(1) **14 天无 review → close**。任何外部 repo 的 PR，提交后 14 天内没收到任何 review（包括 bot review 不算）→ 关闭并在 comment 说明原因。不管 CI 是否绿、不管自己觉得改动多有价值。(2) **APPROVED 未 merge：rule #46 的 3 次 ping 后 → 14 天硬性 close**。不再「降频到每 2 周检查」，直接 close。APPROVED-but-never-merge 的 PR 占 followup 列表但零进展 = 纯成本。(3) **Assigned issue 连续 3 次 followup 说"下次做" → 强制 unassign**。每次 followup 如果只是说"还没开始，下次做"，计数器 +1。累计 3 次 → 去 issue 下说明放弃并请求 unassign。不打算做就别占坑。(4) **去掉 "explicit wait" 逃生门**——之前 maintainer 说 "keep open" 可以豁免 stale-close。现在改为：maintainer 说 keep open → 最多再等 14 天。之后仍无进展 → 关闭并说明 "closing per lifecycle policy, happy to reopen if needed"。判断方法：每轮 followup 先查所有 open PR 的年龄（`gh search prs --author=kagura-agent --state=open --sort=created --order=asc`），>14 天 + 无 review = 关闭候选。推广：softdeadline 是 no-deadline 的变种——硬性限制才是真正的限制。PR limbo 的认知成本是隐性的但真实的：每个 limbo PR 每轮 followup 花 ~30s 确认「球在对方」，50 个 limbo PR × 每天 3 轮 = 每天 75 分钟的纯浪费

51. **Verify external operation claims — don't trust subagent text assertions** — NemoClaw#3836 教训：06-06 subagent 声称已 unassign issue，memory 记录了 "正式 unassign"，但实际 GitHub API 查询显示仍然 assigned。连续 3 天 followup 都基于 memory 中的错误记录跳过了这个 issue，直到 06-09 daily audit 才发现。原则：**subagent 或自己声称执行了外部操作（unassign、merge、close、comment、label 等）→ 必须用 API 验证实际状态**。流程：(1) subagent 返回结果后，用 `gh api` 或 `gh issue/pr view` 验证关键操作是否真的生效 (2) 特别是 mutation 操作（写操作），成功的文本声明 ≠ 实际执行（可能权限不够、API 报错但被 swallow、或 subagent 幻觉）(3) 已建立工具自动化验证：`flowforge/scripts/verify-external-ops.sh` 可验证 6 类外部操作 (4) memory 中的操作记录必须标注 `[已验证]` 或 `[未验证]`——未验证的记录不能作为后续决策依据。判断方法：任何涉及他人 repo 权限的操作（assign/unassign/merge/label）失败概率更高，因为你可能没有写权限。自己 repo 的操作相对可信但仍需抽查。推广：这不只是 subagent 的问题——任何异步操作链（cron → subagent → API）中间环节越多，端到端验证越重要。与 rule #40（honor assignments）互补：#40 防止忘记承诺，本条防止虚假兑现

52. **CI "files modified by hooks" after rebase → re-rebase, don't debug formatting** — NemoClaw#4706 教训（连续 2 次 CI 失败）：rebase 后 CI 报 "Files were modified by following hooks"（biome/prettier/eslint --fix 等），直觉是格式问题去 deep-dive 修格式文件。实际 root cause：upstream main 在你 rebase 之后又 advance 了几个 commits，你的 merge base 已过期，hook 对最新代码的格式期望不同。re-rebase 到 latest upstream/main 后 CI 立即通过。原则：**CI 报 "files modified by hooks" 或 pre-commit format drift → 先 re-rebase 到 latest upstream/main，不要逐文件修格式**。流程：(1) `git fetch upstream` (2) 检查 upstream/main 是否比上次 rebase 时 advance 了：`git log --oneline HEAD..upstream/main | wc -l` (3) >0 → `git rebase upstream/main`，push (4) CI 仍失败 → 才运行 `npx biome format --write` / `prettier --write` / `eslint --fix` 逐文件修。判断方法：如果 CI 只在 rebase 后新出现 "modified by hooks" 而你没改那些文件 → 99% 是 merge base 过期。推广：活跃 repo（>3 commits/day）的 CI 环境在不断漂移——你的 rebase 只是一个快照，快照越陈越可能与 HEAD 不兼容。与 rule #47（sparse checkout break rebase）互补：#47 解决 "rebase 本身失败"，本条解决 "rebase 成功但 CI 失败"

53. **Claim without prompt delivery → get scooped** — opencode#30662 教训：在 issue 下留言 claim，但没及时提 PR，beenotung 提了 PR #31092 覆盖同一 issue，claim 变废纸。Rule #40 覆盖「忘记承诺」，本条覆盖「慢兑现」：活跃 repo 的 issue 有竞争时间窗口，claim 后不交付 = 等人来抢。原则：**claim issue 后 24-48h 内必须提 PR 或至少 push WIP 分支**。流程：(1) 留言 claim 后立即开工，当轮 workloop 优先处理 (2) 如果预计需要 >48h，在 claim comment 里写预计时间（"I'll have a PR within 3 days"），给 maintainer 预期 (3) 48h 到了没 PR → 要么 push WIP PR（draft 状态也算占坑） → 要么在 issue 下说放弃 (4) 被 scoop 后：礼貌 acknowledge，不争、不重开。判断方法：claim 后检查 issue 的 `participants` 和最近 comment——如果有其他人也表达兴趣（"I'd like to work on this"），你的时间窗口更短（≤24h）。推广：claim 不是预订——开源世界没有排他锁。claim 只是礼貌表达意向，PR 才是实际占坑。与 rule #40（honor assignments）互补：#40 防止忘记承诺，本条防止拖延交付

54. **Understand docs framework routing before fixing docs links** — NemoClaw#5108 教训：修了一个 docs link 使其指向正确的 filesystem path，但 repo 使用 Fern docs framework，routing 基于 `docs/index.yml` 中的 route-style slugs 而非 filesystem path。我的 "fix" 实际上会 break 正确的 Fern route，maintainer 直接 close。原则：**修 docs link 前先理解 docs framework 的 routing 机制**。流程：(1) 查 `docs/` 目录下有没有 framework config（`index.yml`, `docusaurus.config.js`, `mint.json`, `mkdocs.yml` 等）(2) 如果有 → 阅读 config 理解 URL routing 规则（很多 framework 的 URL slug ≠ filesystem path）(3) 验证「broken link」是否真的 broken——在 docs 站点上实际访问，而不是在 repo 里 grep (4) 如果 link 在 docs 站点上工作正常 → 不是 bug，不提 PR。判断方法：如果 repo 有 `docs/` + config file + CI/CD for docs deployment → 该 repo 的 docs 是框架管理的，link 语义可能与 filesystem 完全不同。推广：任何「看起来像 bug」的 docs link → 先在实际部署的文档站点验证，再在 repo 验证。文档框架的抽象层会改变 path→URL 的映射关系

55. **Check monorepo for internal utilities before adding external deps** — multica#4095 教训：加了 `linkifyjs` 外部依赖来做 email link detection，reviewer 指出 monorepo 已有 `@multica/ui/markdown/linkify` 的 `detectLinks` 函数，CHANGES_REQUESTED 要求换用内部包。3 行改动本可以避免一轮依赖审查。原则：**在 monorepo 中修改或新增功能前，先搜索内部包是否已有相关实现**。流程：(1) 在写代码前 `grep -r '<keyword>' packages/ libs/ modules/ --include='*.ts' --include='*.js' -l` 搜索相关函数/类 (2) 查 monorepo 的 `package.json` workspace 配置，了解内部包列表（`pnpm ls -r --json | jq '.[].name'`）(3) 检查目标 package 的 `package.json` 已有哪些依赖——如果有个内部包名看起来相关（如 `@org/ui/markdown/linkify`），先读它的 exports (4) 如果内部包有类似功能 → 用内部包，不加外部依赖。判断方法：monorepo 的 review 文化通常强调内部代码复用——新增外部依赖在 monorepo 里的 review 门槛远高于在独立 repo 中。推广：「为什么用外部包而不用我们已有的 X？」是 monorepo PR 最常见的 CHANGES_REQUESTED 之一——提前搜索 30 秒可以避免一轮 review 往返

56. **Re-verify issue state before implementation — selection ≠ commitment** — MCP Inspector#1462 教训：find_work 选中了 #1462，workloop 开始实现，提交时发现 issue 已被 PR #1464 关闭（merged 3h before selection）。整轮实现白费——写代码、跑测试、plan_review 全部无效。原则：**issue 选择后、实现开始前，必须重新验证 issue 状态是 OPEN**。流程：(1) `gh issue view OWNER/REPO#N --json state --jq '.state'` 确认 state=OPEN (2) `gh issue view OWNER/REPO#N --json comments --jq '.comments[-3:] | .[].body'` 检查是否有 "fixed in #N" / "closing as fixed" (3) 如果 selection 和 implementation 之间有时间间隔（>30 min，如 cron 会话中断后恢复），**必须重新验证**——活跃 repo 几小时内就可能有新 PR merge (4) 同时检查是否有 open PR 已 link 到这个 issue：`gh pr list --repo OWNER/REPO --search "#N" --state open` (5) 如果发现 issue 已关闭或已有覆盖 PR → 立即放弃，不要「看看我的方案是否更好」。判断方法：selection → implementation 之间的间隔越长、repo 越活跃 → re-verify 越重要。24h 活跃 repo（openclaw、NemoClaw）的 issue 窗口期可能只有数小时。推广：这是 rule #22（forward-compat shelf life）的镜像——#22 是关于 PR 被 native fix 淘汰，本条是关于 issue 被其他人的 PR 关闭。核心相同：活跃 repo 的 TODO 状态变化速度比你的 review/implementation 速度更快

57. **Study recent merged PRs for reviewer expectations beyond formal gates** — oh-my-pi#2764 教训：PR 收到 review feedback 要求添加 CHANGELOG.md Unreleased entry。CHANGELOG 不在 CONTRIBUTING.md 里、不是 CI check、不是 PR template 必填项——但 repo 的最近 merged PRs 都有 CHANGELOG entry。Rule #19 覆盖了正式 gate（DCO/CLA/commit format），但没覆盖这类「不写在规则里但 reviewer 会要求」的软期望。原则：**首次给新 repo 提 PR 前，看 3-5 个最近 merged PR 的 diff，特别注意它们都改了哪些非代码文件**。流程：(1) `gh pr list --repo OWNER/REPO --state merged --limit 5 --json number,files --jq '.[] | "\(.number): \(.files | map(.path) | join(", "))"'` 列出最近 5 个 merged PR 的文件列表 (2) 如果多个 PR 都改了 `CHANGELOG.md` / `CHANGES.md` / `HISTORY.md` → 你的 PR 也必须加 (3) 如果都有特定 test 目录或文件命名约定（如 `packages/xxx/tests/xxx.test.ts`）→ 你的 test 要放在同样的位置 (4) 如果 PR description 都有固定结构（如 "## What", "## Why", "## Testing"）→ 你也写相同结构 (5) 记录到项目笔记，下次不用重查。判断方法：**任何「最近 3+ 个 merged PR 都做了的事，你的 PR 没做」= 下一轮 review 的必然 feedback**。这类 soft convention 比 CI check 更难发现，但同样会导致 review 往返。推广：Rule #19 检查的是「repo 明文要求你做的事」，本条检查的是「repo 隐含期待你做的事」——两者都能省一轮 review

58. **Check for competing PRs before implementing — don't duplicate work** — hermes-agent#44782 教训：完整实现了一个 CI fix，提交后发现 LeonSGP43 早 4 小时开了 PR #44652 覆盖同一 issue，我的 PR 直接被关 duplicate。整轮选题、分析、实现、测试全部浪费。Rule #56 检查 issue 是否仍 OPEN，但没查是否已有人在做——issue OPEN ≠ 没人在修。原则：**实现前搜索同 issue 的已有 PR**。流程：(1) `gh pr list --repo OWNER/REPO --search "#ISSUE_NUMBER" --state open` 检查是否有 open PR 引用了同一 issue (2) `gh pr list --repo OWNER/REPO --search "<issue_keyword>" --state open --limit 10` 用关键词搜，因为有些 PR 没显式 link issue (3) 同时看 issue 的 timeline：`gh api repos/OWNER/REPO/issues/N/timeline --paginate --jq '.[] | select(.event=="cross-referenced") | .source.issue.pull_request.html_url'` 找交叉引用的 PR (4) 如果发现已有 open PR 覆盖同一问题 → 立即放弃，不要「看看我的方案是否更好」(5) 如果已有 PR 方向不对（修错根因）或覆盖不全，可以在那个 PR 下 comment 指出，而不是开竞争 PR。判断方法：活跃 repo（>5 commits/day）的热门 issue 在发布后数小时内就有竞争 PR 出现。Rule #28 说竞争窗口短，本条给出具体的 pre-check 命令。推广：与 rule #56（re-verify issue state）互补：#56 检查「issue 有没有被关掉」，本条检查「issue 有没有人在做」——两个都要查才能避免白费一轮。与 rule #53（claim without delivery → get scooped）互补：#53 讲 claim 后要快交付，本条讲开工前先看有没有人已经交付了

44. **Resolve CHANGES_REQUESTED before opening new PRs in same repo** — qwen-code#4456/#4474/#4461 教训：同 repo 3+ PRs 同时处于 CHANGES_REQUESTED 状态，每轮 followup 列出来但多天未 push 修复（认知过载 + 维护者印象差——看到你不断开新 PR 但旧 PR 的 review feedback 一直不处理）。原则：**同一个 repo 有未处理的 CHANGES_REQUESTED → 先修再开新 PR**。流程：(1) 开工前 `gh search prs --author=kagura-agent --state=open --repo OWNER/REPO --review=changes_requested` 检查该 repo 是否有待处理 review (2) 有 → 本轮优先 push fix，不开新 PR (3) 修完后 re-request review (4) 只有该 repo 所有 CHANGES_REQUESTED 都处理完（push 了新 commit 或确认无需改动并回复 reviewer），才能开新 PR。判断方法：如果同 repo ≥2 个 PR 同时 CHANGES_REQUESTED 且都超过 48h 未处理，这已经是维护者信任危机——每多拖一天，后续 PR 被认真 review 的概率都在下降。推广：维护者视角看你的 PR 列表是你的「信用报告」——一堆未回应的 review = 不靠谱的贡献者。清理存量 review debt 的 ROI 远高于开新 PR

60. **Write precise test assertions — existence checks prove nothing** — ClawX#1130 教训：5 个新单元测试初版全用 `toBeDefined()` / `toHaveLength(n)` 等存在性断言，fresh-context review 判定 NEEDS_WORK（"weak assertions don't verify fix behavior"），多花一轮编辑才通过。维护者 review 标准更严——弱断言会被要求重写或直接拒绝。原则：**每个 test case 的 assertion 必须验证实际行为/值，不只是验证「有返回」**。流程：(1) 写完测试后自检每个 `expect()` 调用：能否用 `expect(result).toEqual(具体期望值)` 替代 `expect(result).toBeDefined()`？ (2) 对于 error case：`expect(fn).toThrow('具体错误消息')` > `expect(fn).toThrow()` (3) 对于集合：`expect(items).toEqual([{id: 1, name: 'foo'}])` > `expect(items).toHaveLength(1)` (4) 如果值不确定（如 timestamp/UUID），用 `expect.stringMatching(/regex/)` 或 `expect.objectContaining({key: value})` 而不是 `toBeDefined()` (5) 经验法则：**如果 fix 被 revert，你的测试仍然通过 → 测试无效**。判断方法：写完 assertion 后问自己「如果我把 fix 删掉，这个 test 会不会红？」——如果不会，assertion 太弱。推广：弱断言有 3 个成本：(a) 不验证行为 → 不证明 fix 有效 (b) 不捕获 regression → 防护是假的 (c) reviewer 一定会要求改 → 白多一轮 review roundtrip。与 rule #14（test the exact repro）互补：#14 确保你测了正确的场景，本条确保你在那个场景里做了有意义的验证

61. **When a rule excludes a repo, enforce it in tooling immediately — rules without gates are decoration** — hermes-agent#51220 教训（06-23）：rule #59 于 06-20 加入 guide，明确说 hermes-agent (189K⭐) 不可赢，但 gogetajob blocklist 从未添加该 repo。结果：3 天后 workloop 仍然选中 hermes-agent#51220，又花了一整轮 study+plan+implement。规则写了但没落地到工具 = 规则不存在。原则：**每次在 guide.md 新增 repo 排除规则时，同一轮必须执行对应的工具动作**。流程：(1) guide.md 写了「不做 repo X」 → 同一个 session 里跑 `gogetajob blocklist add OWNER/REPO --reason "Rule #N: ..."` (2) 如果排除基于 star count 阈值（如 rule #59 的 >100K⭐）→ 检查 targets.md 和 watchlist 中是否有其他同类 repo 需要一并加入 (3) 如果排除涉及 issue-funnel.sh 的 `--exclude` 参数 → 更新 workloop.yaml 的 find_work 节点默认 exclude 列表 (4) 验证：blocklist add 后跑 `gogetajob blocklist list` 确认生效。判断方法：如果你写完 guide rule 后不做任何工具变更就结束了 session → 你在写文档不是在改行为。推广：这是 AGENTS.md 「建议≠行动」原则在 contribution 领域的实例——guide rule 是建议，blocklist/config 才是行动。与 rule #59（ultra-high-star 不可赢）互补：#59 说什么 repo 不做，本条说怎么确保不做的决定被 enforce

59. **Ultra-high-star repos (>100K⭐) are systematically unwinnable — don't select them** — hermes-agent (189K⭐) 教训：across 6+ attempts spanning weeks (issues #49307, #49462, #44782, etc.), 0 PRs merged. Every attempt ended in SUPERSEDED (competitor beat us by hours), duplicate (someone already working), or simply ignored. This isn't bad luck — it's structural: (1) Ultra-high-star repos have large core teams that self-fix issues faster than any external contributor can implement + review (2) The issue competition window is sub-hour — by the time flowforge runs study→plan→implement, someone already shipped (3) Multiple competing contributors per issue means even correct implementations get duplicated (4) Core teams prefer their own style/architecture, making external PRs inherently disadvantaged regardless of quality. 原则：**repo selection 时检查 star count + external merge rate 作为 hard gate**。流程：(1) `gh api repos/OWNER/REPO --jq '.stargazers_count'` — if >100K, apply extra scrutiny (2) `gh pr list --repo OWNER/REPO --state merged --author-is-not:TEAM_MEMBERS --limit 10` — check if non-team contributors get merged recently (3) Check your own track record with this repo: 0 merged across ≥3 attempts = hard stop (4) If selecting anyway (rare exception for tiny/mechanical fixes), time-box to ≤2h total including study — don't invest full workloop cycles. 判断方法：>100K⭐ + 0 external merges in last 30 days = unwinnable. >50K⭐ + your personal 0-merged track across 3+ attempts = unwinnable for you specifically. 替代策略：same codebase knowledge → contribute to smaller forks/ecosystem tools that reference the mega-repo; write blog posts/analysis instead of PRs. 推广：Rule #28 说竞争激烈要快，rule #58 说查 competing PRs，但本条说的是**有些战场无论怎么快都赢不了**——「不打」比「打快」更重要。与 rule #9（repeat supersede → blocklist）互补：#9 针对「明确拒绝」，本条针对「结构性无法赢」——两者动机不同但结论相同：停止投入

62. **Audit shared mechanism blast radius — your fix target may be someone else's dependency** — openclaw#96371 教训：PR 修改了 error suppression 路径来解决一个 UX 问题（tool exit code != 0 时 deliver callback 被压制），但 reviewer 发现：(1) 同一个 suppression flag 也被 heartbeat path 使用 → 安全隐患，可能暴露内部错误细节 (2) patch 把 static config (`suppressToolErrorWarnings`) 和 dynamic dedup 逻辑混在一起 → 需要拆分。Fix 对目标 case 是正确的，但没检查同一机制的其他消费者。原则：**当你的 fix 修改了一个共享机制（config flag、error handler、middleware、callback path），必须显式审计该机制的所有调用方/消费者，检查是否有非预期副作用**。流程：(1) 提 PR 前 `grep -rn '<mechanism_name>'` 找出所有使用点 (2) 对每个使用点问："我的改动如果也作用到这里，效果是否可接受？" (3) 如果任何使用点在你的改动下会产生不良行为 → 你需要拆分机制（为不同消费者走不同路径） (4) PR description 里列出 blast radius："This change affects paths A, B, C. Effect on A (intended): X. Effect on B (reviewed, no impact): Y. Effect on C (needs separate path): Z"。判断方法：如果你的 fix 修改了一个**名字里有 "suppress"、"filter"、"ignore"、"skip"、"handle"** 的函数/flag → 大概率是共享机制，需要审计。推广："共享机制"包括：config flags、error handlers、middleware chains、event listeners、callback paths、env vars，以及任何 >1 个消费者的代码路径。fix 范围越窄越好——但如果 fix 碰到了共享路径，你**必须**列举消费者。与 rule #12（fix all code paths）互补：#12 确保你修完了所有坏的路径，本条确保你没搞坏不该碰的路径

63. **In competitive repos, narrow proof beats broad scope — speed is the first filter** — openclaw#96981 教训：issue#96878（plugin install fallback）选题 12:35、plan approved 12:39、实现提交 16:02，但到 20:52 被 snowzlmbot 的 PR#96987 supersede——对方更窄的 fix + 真实 install 输出证明，ClawSweeper 直接关了我们的。整个 cycle ~4h 太慢，而对方提交了一个更简单但立即可验证的修复。Rule #22 说 fast-moving repo 的 PR 有 shelf life，rule #58 说查 competing PRs，但本条说的是**scope 策略选择**：同一个 issue，broad 方案需要多轮 review 验证而 narrow 方案自证完备——在有 bot reviewer 和快节奏的 repo 里，narrow wins。原则：**在高活跃 repo（>5 commits/day 或有 bot reviewer），优先选择最小 scope + 即时可验证的方案，不追求「更完整的设计」**。流程：(1) 评估 fix scope 时问：能否把方案砍到 1 file / <20 lines 且自证正确（terminal output / test result）？ (2) 如果有 bot reviewer（ClawSweeper、CodeRabbit 等），它们评审 proof > code quality——提供 real behavior proof（安装输出、CLI 运行截图、before/after diff）比完美架构更容易过 (3) 如果你的方案需要 2+ rounds review 才能证明安全（如需要拆分机制、补测试、加 guard），而 issue 在活跃 repo → 考虑先提最小 fix 占坑，后续开 followup PR 补完整方案 (4) Time-box：高竞争 issue 从 claim 到 PR 提交不超过 2h，超过说明 scope 太大。判断方法：如果你的 PR description 需要 >3 段来解释为什么这样做 → scope 可能太大。如果竞争者可以用 5 行代码 + 一段 terminal output 解决同一问题 → 你的方案过度工程了。推广：这不是说永远选最小方案——在没有竞争的 repo（dormant、solo-maintainer）里，broad+完整是正确选择。本条只适用于**有活跃竞争**的场景。与 rule #53（claim without delivery → scooped）互补：#53 说要快交付，本条说交付的**内容**也要够窄才能快

65. **Address review feedback within 24h — stale rework invites competitors** — openclaw#96371 + #96981 教训：#96371 收到 ClawSweeper review 06-24 指出需要拆分 suppression 路径，3 天后（06-27）仍未 rework——PR 一直挂着 unresolved feedback。#96981 同周收到 review 要求补 scoped npm path + expectedPluginId，还没来得及 push fix 就被 snowzlmbot 的 PR#96987 supersede。memex#173 Windows CI 失败也跨多个 workloop 周期未修复——红 CI 的 PR 等于不存在。Rule #44 说先处理 CHANGES_REQUESTED 再开新 PR，rule #63 说初始提交要快，但本条说的是**rework 阶段同样有竞争时间窗口**：reviewer feedback 是公开的（尤其 bot review），竞争者能看到你 PR 的弱点并提交更好的方案。原则：**收到 review feedback → 24h 内 push fix 或回复 reviewer 说明计划，不要等「下一轮 workloop」**。流程：(1) GitHub patrol 发现 CHANGES_REQUESTED / bot review suggestions → 当轮 workloop 最高优先 (2) 如果 rework 需要 >2h（如需要拆分机制、补大量测试）→ 在 PR comment 里写明预计完成时间（"I'll push the fix within 48h"），防止 reviewer 以为 PR 被放弃 (3) CI failure（红 badge）也是 feedback 的一种——PR 带红 CI 维护者不会 review，等于自动被忽略。修 CI > 写新 feature (4) 如果发现 rework 太复杂 → 考虑 close 当前 PR，基于 reviewer feedback 重新开一个更窄的 PR（faster than patching a complex PR）。判断方法：reviewer feedback 超过 48h 未 addressed + repo 仍活跃（有新 commit/PR） → 你的 PR 正在被遗忘。推广：review feedback 是 PR 的「第二次 claim」——初次提交占坑，reviewer feedback 后重新计时。与 rule #53（claim without delivery → scooped）互补：#53 管 issue→PR 速度，本条管 review→rework 速度。与 rule #63（narrow proof beats broad scope）互补：#63 管初始 scope 选择，本条管 review 后的响应速度

66. **Repos introduce new contribution gates mid-stream — re-check policy before returning after a gap** — oh-my-pi#3703 教训：PR 被 vouch system 自动关闭。该 repo 于 06-19 引入 vouch gate（需要已有贡献者担保才能 merge），但我们基于之前的贡献经验（#2764 正常 review 中）直接提了 #3703，没查新 policy。Rule #19 覆盖首次贡献的 gate 检查，但不覆盖**回头贡献时 repo 已改变规则**的情况。原则：**回到一个 >14 天没提 PR 的 repo 前，重新检查贡献政策是否变化**。流程：(1) 看 CONTRIBUTING.md / `.github/` 有没有近期修改：`gh api repos/OWNER/REPO/commits?path=CONTRIBUTING.md&per_page=3 --jq '.[0].commit.committer.date'` (2) 看最近 closed PR 有没有 bot/gate 自动关闭的 pattern (3) 搜索 repo 有没有新 bot（vouch bot、sponsor-only gate、triage bot 等）：`gh api repos/OWNER/REPO/issues?labels=bot&per_page=5` 或看最近 issue/PR comments 的 author (4) 如果发现新 gate（vouch、sponsor-gate、approval-required）→ 先满足 gate 条件再写代码（如 vouch：开 Discussion 请求 vouch）(5) 更新项目笔记记录新 gate。判断方法：如果你的 PR 被一个你没见过的 bot/workflow 自动关闭 → 100% 是新 gate，先搞清楚规则再重试。推广：开源 repo 的贡献政策是活文档——尤其快速成长的项目（star 涨幅 >50%/月）更可能收紧外部贡献门槛（anti-spam、quality gate、vouch system）。与 rule #19（first-time gate check）互补：#19 管首次准入，本条管回访准入。与 rule #29（anti-AI sentiment）互补：#29 管 AI 贡献者的专项歧视，本条管通用的 policy 变更

67. **Trace new params/config end-to-end before submitting — each unwired gap costs a full review round** — qwen-code#5957 教训：添加了 `reservedOutputTokens` 参数支持 maxOutputTokens 功能，但提交时存在 4 个独立缺陷，每个被 reviewer (wenshao) 在不同轮次发现：(1) 函数签名变了但 `client.test.ts` 里的调用没更新 → 测试直接挂 (2) env var `QWEN_CODE_RESERVED_OUTPUT_TOKENS` 定义了但值从未被读取 → 运行时永远是 0 (3) ACP code path 没有 propagate 新参数 → 该路径功能失效 (4) 没有为新 behavior tier (hard limit) 写测试 → 无法证明功能有效。5 轮 CHANGES_REQUESTED，每轮修一个洞，总耗时 5 天。原因：提交前没做 E2E trace。原则：**当你的 PR 引入新 param/config/flag，提交前必须做完整的 data-flow trace：定义 → 读取 → 传播到所有消费者 → 实际生效 → 测试覆盖**。流程：(1) 画出数据流：env/config → 读取点 → 传递链 → 最终使用点。每一跳都必须有代码对应 (2) 对每个使用点写一个 test case 验证参数实际生效（不是 toBeDefined，是行为变化）(3) `grep -rn '<new_param_name>'` 检查所有引用——如果某个文件 import 了相关模块但没使用新参数 → 可能是遗漏的传播点 (4) 跑现有 test suite 确认没有 break——函数签名变了，所有 caller 的参数列表都要更新 (5) 自检 checklist：「如果我把新 param 删掉，哪些测试会红？」如果答案是 0 → 你的覆盖不够。判断方法：如果你的 PR 改了函数签名或增加了 env var，而你没有 `grep` 过所有 caller / 没有验证 env 值被实际读取 → 大概率有洞。5 轮 review 的成本（calendar time + reviewer goodwill）远高于提交前 30 分钟的 trace audit。推广：这不只是 config/env var——任何「引入新数据路径」的 PR 都适用：新字段、新 flag、新 option。核心问题永远是「我引入了 A，A 到达最终消费者了吗？」。与 rule #12（fix all code paths）互补：#12 确保你修了所有坏的路径，本条确保你新加的东西在所有路径都通了。与 rule #60（precise test assertions）互补：#60 确保测试有意义，本条确保测试存在

64. **Narrow error handling beats catch-all — match the exact error you expect** — NemoClaw#5740 教训：我的 fix 用了 broad try/catch 包住 orphan-manifest 读取，cjagwani 开了 PR#5819 只 catch regex-matched orphan-manifest 错误。我的方案会吞掉 disk-full、SSH-timeout、permission-denied 等真实失败——看似更「安全」实际更危险。被 supersede。原则：**error handling 只 catch 你能命名的、预期的错误类型，用 regex/error code/class 精确匹配**。流程：(1) 写 try/catch 前先列出这里可能出现的所有错误类型 (2) 只 catch 你的 fix 需要处理的那 1-2 种（如 `ENOENT` for file-not-found, specific error message pattern for known benign failure）(3) 所有其他错误 → re-throw 或不 catch (4) 如果你写了 `catch (e) { /* ignore */ }` 或 `catch (e) { return fallback }` → 问自己：disk full 走到这里会怎样？网络超时呢？如果答案是「也被吞掉」→ 你的 catch 太宽了。判断方法：**如果 catch block 不检查 error 的类型/消息/code 就直接处理 → 太宽**。正确模式：`catch (e) { if (e.message.match(/orphan manifest/)) { /* handle */ } else { throw e } }` 或 `catch (e) { if (e.code === 'ENOENT') { /* handle */ } else { throw e } }`。推广：broad catch-all 是「防御性编程」的错误应用——真正的防御性编程是让意外错误暴露出来（fail-fast），而不是让它们消失（fail-silent）。与 rule #62（blast radius audit）互补：#62 检查「你的改动影响了哪些其他路径」，本条检查「你的改动会不会吞掉不该吞的信号」

68. **Fork-origin PRs are structurally disadvantaged in repos with fork-restricted CI — check before investing** — NemoClaw#5983 教训：PR 代码完全正确（wscurran 人类 reviewer 确认 "Thanks for the fix"），但被 #6023 supersede，因为 NemoClaw 的 PR Review Advisor CI 明确 "fork-origin advisor jobs are skipped by policy"。same-repo contributor (cv) 重建了同样的 fix + 扩展为模块化架构（15 files vs 我的 2 files），CI 能正常跑。结果：我的正确代码因为无法通过 CI 验证而被淘汰。Rule #19 覆盖 contribution gates（CLA/DCO），rule #66 覆盖新 gate 出现，但本条覆盖的是**CI 本身对 fork PR 的结构性歧视**——不是 gate 拦你，是 CI 压根不跑。原则：**提 PR 前检查目标 repo 的 CI 是否对 fork PR 有功能限制（不跑、部分跑、需 maintainer approval 才触发）**。流程：(1) 看最近 5 个 fork-origin PR（author 不在 team 里）的 CI status：如果都是 "skipped" 或 "pending" 而 same-repo PR 都绿 → fork CI 受限 (2) 检查 `.github/workflows/` 里有没有 `if: github.event.pull_request.head.repo.full_name == github.repository` 或类似条件 (3) 看 repo 有没有 mandatory status checks 或 bot reviewers 需要 CI pass 才评审（如 NemoClaw 的 PR Review Advisor）(4) 如果 fork CI 受限 → 评估风险：你的 PR 永远无法自证 CI-clean，same-repo contributor 随时可以复制你的思路并跑出绿 CI supersede 你。应对：(a) 小 fix（<20 lines）接受风险，提了就走 (b) 大 fix（需要架构决策或多文件）→ 不值得——fork PR 的 review 门槛更高（reviewer 无法依赖 CI，必须人工验证） (c) 如果你已经有 repo write access 或能请求 contributor 权限 → 转为 same-repo branch PR。判断方法：你的 fork PR 提交后 CI 显示 "skipped" 或需要 maintainer manual trigger → fork CI 受限。如果同时该 repo 有 bot reviewer 依赖 CI status → 你的 PR 在审查队列中永远排在 same-repo PR 后面。推广：fork CI 限制是 2024+ 越来越多 repo 采用的安全措施（防 CI 资源滥用），但对外部贡献者造成结构性不公平。这不是歧视，是安全 tradeoff——但作为贡献者你需要知道这个 tradeoff 存在并据此决策。与 rule #59（ultra-high-star unwinnable）互补：#59 说大 repo 因竞争不可赢，本条说即使小/中 repo 也可能因 CI 政策不可赢。与 rule #63（narrow proof beats broad scope）互补：如果决定在 fork-CI-restricted repo 提 PR，更应该选最窄 scope + 自带 proof（降低 reviewer 不信任 CI 的阻力）

70. **Identify bot reviewers vs human reviewers — triage rework priority accordingly** — qwen-code#6155/#6104 教训：review pipeline 使用多个 AI bot 以人类用户名提交 review（doudouOUC = qwen3.7-max，wenshao = automated review bot），与明确标记的 bot（qwen-code-ci-bot、CodeRabbit、ClawSweeper）不同。不识别 bot vs human 导致：(1) 对 bot 的 formulaic nitpick 投入与 human critical 同等精力 (2) 试图「说服」bot 改变 review 意见（bot 不读你的回复，下次 push 自动重新 review）(3) 误以为 bot APPROVED 等于即将 merge（实际 merge 决定权在 human maintainer）(4) 把 human 反馈当 bot 忽略（错过真正需要处理的 feedback）。qwen-code#6155 同时收到 doudouOUC(bot) CHANGES_REQUESTED 和 DragonnZhang+qqqys(human) APPROVED——bot 的 build failure 需修但 stylistic suggestions 可降优先级。原则：**首次给新 repo 提 PR 时，识别每个 reviewer 是 bot 还是 human，据此调整 rework 优先级**。流程：(1) 看 reviewer 的 profile：bot 通常有 `[bot]` 后缀、机器头像、或 bio 里标注 (2) 看 review body 的 footer/signature：AI review bot 通常附带生成模型标记（如 `<!-- generated by qwen3.7-max -->`、CodeRabbit 的 rabbit emoji footer）(3) 看 review pattern：同一 reviewer 在多个 PR 上秒回、review 结构高度一致（总是 N Critical + M Suggestion）、使用 checklist 模板 → 大概率是 bot (4) 记录到项目笔记：`reviewers: { human: [DragonnZhang, qqqys], bot: [wenshao(qwen3.7-max), doudouOUC(qwen3.7-max), qwen-code-ci-bot] }` (5) Rework 优先级：human CHANGES_REQUESTED > bot Critical (build failure) > human Suggestion > bot Suggestion/nitpick。判断方法：如果一个 reviewer 在你 push 后 30 秒内就发了新 review → 几乎肯定是 bot。如果 review 的语言和格式与其他 repo 的已知 bot（CodeRabbit、ClawSweeper）高度相似 → bot。如果 reviewer 的 GitHub 活动全是 review comment 没有自己的 commit → bot account。推广：AI code review bot 正在快速扩散——2026 年主流 repo 至少有 1 个 bot reviewer（CodeRabbit、Copilot Review、ClawSweeper、自建 LLM review bot）。bot review 不是坏事（能抓 build failure、格式问题），但不应该与 human review 混淆处理。**bot 的 CHANGES_REQUESTED 不阻止 merge——human maintainer 的 approval 才阻止 merge**。与 rule #44（resolve CHANGES_REQUESTED before new PRs）互补：#44 管 review debt 清理优先级，本条管**同一 PR 内**的 review 来源分类。与 rule #69（holistic re-review after 2+ rounds）互补：#69 管 rework 质量，本条管 rework 对象选择——先搞清楚哪些 feedback 来自 bot、哪些来自 human，再决定投入多少精力

71. **Verify target code/files exist before implementing — some issues are specs for unbuilt features** — NemoClaw#6236 教训：issue 描述了对 `tools/independent-approval/github.mts` 的安全修复，但这个文件在代码库中根本不存在——issue 实际上是一个 draft implementation 的规格说明，不是现有代码的 bug。在 study 阶段投入时间分析 issue、研究代码结构后才发现这一点。Rule #56 检查 issue 是否 OPEN（它是 OPEN 的），rule #49 检查 comments（comments 没问题），rule #58 检查 competing PRs（没有），但没有规则检查**issue 引用的代码是否已经存在**。原则：**study 阶段的第一步是验证 issue 引用的文件/函数/模块在代码库中实际存在**。流程：(1) 读 issue body 提到的文件路径、函数名、模块名 (2) `find . -name '<filename>' -o -path '*<path_fragment>*'` 或 `grep -rn '<function_name>'` 确认目标代码存在 (3) 如果目标代码不存在 → 检查 issue 标签和 context：是否标记为 `enhancement` / `feature-request` / `design` / `spec`？是否引用了尚未 merge 的 PR？ (4) 如果 issue 是对**尚未实现的功能**提需求/改进 → 放弃，不是可实现的 bug fix (5) 例外：如果 issue 要求**创建**新文件（如 "add missing config file"、"create migration script"）→ 目标代码理应不存在，这不是阻止项。判断方法：issue body 用「修复」「改进」「重构」描述一个**已存在的组件**但你找不到它 → 大概率是 spec for future code。issue body 用「添加」「创建」「实现」描述新东西 → 目标代码不存在是预期的。推广：快速成长的 repo 经常有「设计 issue」先于实现——maintainer 先写 issue 描述想法，后续才写代码。这些 issue 状态是 OPEN、标签可能是 bug 或 enhancement、内容看起来像可执行的 fix spec——但目标代码不存在。与 rule #56（re-verify issue state）互补：#56 检查 issue 的 metadata 状态，本条检查 issue 引用的**代码**状态。与 rule #49（re-read all comments）互补：#49 确保你理解 issue 的完整讨论，本条确保你理解 issue 的前提条件——目标代码是否存在

72. **When upstream architecturally refactors your target file/module, close or re-implement — don't force rebase** — NemoClaw#6211 教训：PR 修改了 `rebuild.ts`（~1400 行），review 期间 upstream 将整个文件拆分成 ~50 个子模块。Rebase 尝试产生 total conflict（lines 4-1405），几乎每一行都冲突，不可能手动 resolve。最终通过从 upstream/main 重建分支、重新 apply 改动才解决，但如果改动涉及已不存在的函数/结构，重新 apply 等于从头写。Rule #22 覆盖「PR shelf life」，rule #47 覆盖「sparse checkout break rebase」，但本条覆盖的是**目标代码被整体重构**——不是几行冲突，是整个文件/模块的架构变了。原则：**rebase 失败且 conflict 覆盖目标文件 >50% → 不要逐行 resolve，做二选一决策：close 或 re-implement**。流程：(1) `git rebase upstream/main` 后看 conflict 范围——如果整个目标文件（或你改动的主要文件）都是 conflict marker → 架构级重构 (2) `git log --oneline upstream/main -- <file>` 看 upstream 最近对该文件做了什么——如果有 "refactor"、"split"、"extract"、"modularize" 类 commit → 确认重构 (3) **快速评估**（≤15 min）：你的原始修改逻辑在新架构里是否仍然有意义？改动的函数/类是否仍存在（可能被重命名/拆分到新文件）？ (4) 如果逻辑仍有意义 → `git rebase --abort`，从 upstream/main 创新分支，在新架构里重新定位并实现修改（不是逐行 transplant，是理解新结构后重新写）(5) 如果逻辑已不适用（重构改变了 bug 的根因或 feature 的实现方式）→ 关闭 PR，在 comment 说明 "upstream refactored the target module; closing as the approach no longer applies to the new architecture"。判断方法：**conflict 行数 / 文件总行数 > 0.5 + upstream 有 refactor 类 commit → 架构级重构**。普通 conflict（<20% 行冲突）可以逐行 resolve；架构级 conflict 逐行 resolve 会产生语义不正确的合并（函数调用指向已不存在的模块）。推广：活跃 repo（>10 commits/day）在你 PR review 期间做大重构的概率与等待时间正相关——与 rule #65（24h 内 address feedback）互补：review 越快、等待越短、被重构淘汰的概率越低。与 rule #22（shelf life）互补：#22 说 PR 会过期，本条说过期的**具体表现之一**是目标代码架构变了。与 rule #47（sparse checkout rebase）互补：#47 解决 sparse checkout 导致的 rebase 失败，本条解决 upstream 重构导致的 rebase 失败——成因不同，应对相同（重建分支）

73. **Recognize competitive deadlock among sibling PRs — cut losses instead of chasing** — openclaw#99047 教训：PR 提交后发现同一 fix 有多个 sibling PR（#99047, #99053 等），其中 #99053 已附带 live behavior proof（终端输出截图），而我们的 PR CI 有 flaky test failure（不相关但外观减分）。结果：竞争性死局，#99053 凭更强的证据被优先 review，我们的 PR 最终只能自行关闭。Rule #58 管「开工前查 competing PRs」，rule #63 管「初始 scope 选择」，但本条管的是**提交后发现自己处于劣势的多方竞争**——如何识别败局并及时止损。原则：**PR 提交后发现 ≥1 个 sibling PR 覆盖同一 issue 且有更强证据 → 立即 close，不要追加 proof 试图追赶**。流程：(1) PR 提交后 24h 内检查是否出现新的 sibling PR：`gh pr list --repo OWNER/REPO --search "<issue_keyword>" --state open` (2) 如果发现 sibling PR，评估竞争力：对方有 live proof 而你没有？对方是 same-repo contributor（CI 能跑）而你是 fork？对方 scope 更窄更容易 merge？ (3) 任何 2+ 项劣势 → 立即自行关闭，comment 说明 "closing in favor of #N which has stronger proof/coverage" (4) 不要试图追加 proof 来追赶——追加过程中对方可能已被 merge (5) 把时间投入到无竞争的 issue 上，ROI 更高。判断方法：**你的 PR 没有 live proof + sibling PR 有 = 你已经输了**。在有 bot reviewer（ClawSweeper 等）的 repo 尤其如此——bot 按 proof 排名，不按提交时间。追加 proof 的时间成本（设置环境 + 截图 + push + 等 re-review）通常 >2h，这 2h 足够在另一个 repo 提交一个无竞争的 PR。推广：沉没成本谬误在 PR 竞争中尤其严重——"我已经写了这么多代码" 驱动你继续投入，但代码是沉没成本，reviewer attention 才是稀缺资源。与 rule #58（check competing PRs before implementing）互补：#58 防止一开始就进入竞争，本条处理「竞争是提交后才出现的」场景。与 rule #63（narrow proof beats broad scope）互补：#63 管初始策略，本条管动态退出决策。与 rule #53（claim without delivery → scooped）互补：#53 管速度，本条管「即使你交付了，对方交付得更好时怎么办」

74. **When error strings reference external dependency namespaces, verify fix surface is in this repo — not upstream** — NemoClaw#6287 教训：issue 描述了 sandbox relay token expiry 错误，但所有错误字符串（`RefreshSandboxToken`, `openshell_core::grpc_client`）都指向 OpenShell Rust 模块。NemoClaw TypeScript 源码中零 relay token 生命周期管理代码——NemoClaw 只是调用方，token refresh 逻辑完全在 OpenShell upstream。study 阶段 grep 了整个代码库确认后 withdraw。Rule #71 检查「issue 引用的文件是否存在」，但 #6287 的问题更微妙：文件可以存在（NemoClaw 有 sandbox 相关的 TS 代码），但 fix 的实际 surface 在另一个 repo/crate/package。原则：**study 阶段当 issue 的错误消息、堆栈跟踪或模块引用包含外部依赖的 namespace（crate::module、@org/package、import path）时，验证 fix surface 是在本 repo 还是在依赖的 repo**。流程：(1) 提取 issue 中的错误字符串、模块路径、crate/package 名 (2) `grep -rn '<module_name>'` 在目标 repo 源码中搜索——如果只出现在依赖引用（Cargo.toml、package.json、import 语句）中而不在业务逻辑中 → fix surface 是上游 (3) 检查 issue 引用的函数/结构体是否在目标 repo 中有实现——如果只是 FFI/RPC 调用方 → 本 repo 无法修复 (4) 如果确认 fix 在上游 → 在 issue 下礼貌说明 "This appears to be an upstream issue in [dependency]; the error originates from [module_path] which is outside this repo's codebase"，然后 withdraw (5) 如果上游也是开源的 → 考虑去上游 repo 提 PR 而不是在调用方 repo 打 workaround。判断方法：**issue 的错误字符串中有 `::` (Rust crate path)、`@scope/package` (npm)、或完整模块路径且该路径不在 `src/` 目录结构内 → 大概率是上游问题**。常见陷阱：maintainer 有时在自己的 repo 开 issue 跟踪上游 bug，标签可能是 `bug` 而不是 `upstream`——看起来像可修的 bug 但实际需要上游修。推广：调用方 repo 对上游 bug 能做的通常只有 workaround（catch + retry、fallback），如果 issue 没明确说要 workaround 且你也不确定正确的 workaround 策略 → 不要猜，留给 maintainer 决定。与 rule #71（verify target code exists）互补：#71 检查「代码是否存在」，本条检查「代码是否在正确的 repo 里」。与 rule #62（blast radius audit）互补：#62 检查你的 fix 影响了哪些消费者，本条在更前一步——确认你修的东西本来就在你 PR 的 repo 里

75. **Verify technical claims before posting issue analysis — repeated public corrections destroy credibility** — NemoClaw#6277 教训：在 issue 下分析 Hermes dashboard 端口处理逻辑，被 oparoz（maintainer）纠正两次——两次都是从架构抽象推理而非读实际代码。maintainer 明确要求聚焦 `NEMOCLAW_DASHBOARD_PORT` 但我的分析偏离了目标。issue 上被同一 maintainer 连续纠正两次 = 该 maintainer 对你的技术判断不再信任，后续 PR review 门槛隐性升高。Rule #49 覆盖「开工前读 comments」，rule #14 覆盖「测试 exact repro」，但本条覆盖的是**在 issue 上发布技术分析本身的质量**——这不是 PR pre-check，是你在社区建立或摧毁技术信誉的过程。原则：**在 issue 下发布技术分析前，必须通过读源码验证你要说的每个技术断言，不能从架构图/文档/推理得出结论**。流程：(1) 读 issue 完整讨论 + maintainer 之前的所有 comment (2) 对每个技术断言：`grep -rn '<variable/function/config>'` 在源码中确认 (3) 如果已经被纠正过 → 标准加倍：引用具体文件+行号+代码片段，让 maintainer 看到你确实读了代码 (4) 如果你不确定某个技术细节 → 不写，或明确标注 "I'm not sure about X, but..." (5) 经验法则：如果你的分析可以不读代码也能写出来（纯靠 README/docs/architecture 推理）→ 没做足功课 (6) 特别注意 config/env var 的实际传播路径——config 变量名存在 ≠ 它被使用/传播/生效（与 rule #67 E2E trace 同理，但应用在 issue analysis 而非 PR 代码）。判断方法：被同一 maintainer 纠正 ≥2 次 = 分析方法有系统性问题（不是偶然失误），必须从推理转向读码再发下一条 comment。推广：issue comment 是公开的、永久的。PRs 可以 close 掉，但 issue 上的错误分析永远挂在那里。维护者在 review 你的下一个 PR 时会记得「这个人之前的分析都是错的」——pre-review bias 比 PR 代码质量更难克服。与 rule #49（re-read all comments）互补：#49 确保你不遗漏讨论上下文，本条确保你基于源码而非推理发布分析。与 rule #67（E2E trace）互补：#67 管 PR 代码的参数传播验证，本条管 issue comment 的技术断言验证——同一原则（验证优先），不同应用场景

76. **Check for unresolved design discussions before implementing — design-phase issues aren't ready for code** — oh-my-pi#4685 教训：issue 看起来可实现（OPEN, 有清晰需求描述, 代码可改），但 collaborator roboomp 明确说 "I would wait for maintainer direction before cutting code"，有 5 个未决设计方向。直到 study 阶段读完 comments 才发现——study 投入已被部分浪费。Rule #49 覆盖「读 comments 理解 root cause 变化」，rule #71 覆盖「验证目标代码存在」，但没有规则覆盖「issue 仍在设计讨论阶段，不应开始实现」——issue OPEN ≠ implementation-ready，有时只是 discussion-ready。原则：**study 阶段检查 issue comments 是否有未解决的设计问题——如果有 collaborator/maintainer 建议等待方向确定，不要写代码**。流程：(1) 读完 issue comments 后搜索信号词："wait for", "need to decide", "design decision", "open question", "RFC", "should we", "which approach", "before cutting code" (2) 如果有 collaborator/maintainer 的 comment 包含上述信号 + issue 的方向还没确定 → 不可实现 (3) 特别注意「有多个 comment 提出不同方案但没有最终结论」的 pattern——这是活跃的设计讨论，过早实现任何一个方案都可能白费 (4) 如果你确认 issue 在设计阶段 → 可以参与讨论（提出你的方案分析），但不要开始写代码——等 maintainer 做出方向选择后再实现 (5) 已有明确 maintainer 选择的 comment（如 "let's go with approach B"）→ 可以开始实现 approach B。判断方法：**如果 issue 的 comments 中有 ≥2 个不同的实现方案被讨论且没有 maintainer 最终决定 → 设计阶段**。如果最近一条 comment 是 maintainer 说 "wait"、"hold off"、"need to discuss" → 明确阻塞。如果 issue body 本身就列了多个 options 让人选 → design discussion，不是 implementation spec。推广：大型 repo 的 issue 生命周期通常是 filed → triaged → design discussion → direction chosen → implementation-ready → assigned → implemented。你可以在 design discussion 阶段参与讨论，但 issue 要到 direction chosen 阶段才值得写代码。越早识别设计阶段，越少浪费 study/plan/implement 时间。与 rule #49（re-read all comments）互补：#49 确保你不遗漏讨论，本条确保你从讨论中正确判断 issue 的生命周期阶段。与 rule #71（verify target code exists）互补：#71 检查代码存不存在，本条检查 issue 本身是否处于可实现状态

77. **Design regression test parameters to cross the bug's activation boundary — tests that pass before AND after the fix prove nothing** — openclaw#105120 教训：PR 修复了 heartbeat schedule advance 使用 stale timestamp 的 bug（30min interval），但回归测试用 20s delay 模拟 runOnce 后的 advanceAgentSchedule 调用——20s 根本不会跨越 30min phase boundary，测试无论有没有 fix 都会通过。ClawSweeper bot review 直接指出 "test doesn't cross phase boundary, doesn't demonstrate regression"。Rule #60 覆盖 assertion 质量（toBeDefined vs toEqual），rule #14 覆盖测 exact repro，但本条覆盖的是**测试场景参数的设计**——assertion 可以精准，但如果 scenario 的输入参数根本不触发 bug condition，测试就是假的 green。原则：**回归测试的参数必须精确复现 bug 的触发条件——测试在 parent commit（fix 前）必须 fail，在 fix 后必须 pass**。流程：(1) 实现 fix 前先明确 bug 的触发条件（如「advanceAgentSchedule 在 runOnce 后用了 cached timestamp 而非 Date.now()，当 interval > cache age 时 schedule 不前进」）(2) 设计测试参数使其精确跨越触发边界（如果 bug 是 30min interval 的 phase boundary 问题，测试的 mock 时间必须跨越 30min）(3) **Revert check**：`git stash && npm test <regression_test> && git stash pop` — 如果测试在没有 fix 的情况下也通过 → 参数没触发 bug，重新设计 (4) 对于时间相关的 bug：mock Date.now() 或使用 fake timers 控制时间前进量，不要依赖 setTimeout 的实际延迟（20s delay 在 unit test 里既慢又不精确）(5) 对于阈值/boundary bug：测试至少两个 case——刚好不触发（boundary - 1）和刚好触发（boundary + 1）。判断方法：**如果你的回归测试只验证「fix 后行为正确」但不验证「fix 前行为错误」→ 它不是回归测试，只是功能测试**。回归测试的核心价值是「确保 bug 不会再次出现」——如果它无法检测 bug 的存在，这个价值是零。推广：bot reviewer（ClawSweeper 等）会检查测试是否 demonstrate the fix——如果测试参数明显不触发 bug condition，bot 会自动判定 needs-work。提供 "before/after" proof（parent commit 上 test fail + fix 后 test pass 的 terminal output）是最强的 regression 证明。与 rule #60（precise assertions）互补：#60 确保你在测 meaningful values，本条确保你在 meaningful scenario 里测。与 rule #14（test exact repro）互补：#14 确保你测了正确的场景格式，本条确保场景参数跨越了 bug 边界。与 rule #63（narrow proof beats broad scope）互补：在有 bot reviewer 的 repo，一个 demonstrable regression test 比 10 个不触发 bug 的功能测试更有说服力

69. **After 2+ review rounds, re-review the full diff holistically — incremental patching creates blind spots** — qwen-code#6104 教训：PR 收到 Round 1 CHANGES_REQUESTED，逐条修复后 push，Round 2 又冒出 5 Critical + 4 Suggestion，再修，Round 3 又 4 Critical。每轮只 fix 被点名的 comment，从不退后一步重看整体——结果每次修复要么引入新缺陷（改了 condensed path 但漏了该 section 的其他 constraint），要么暴露出 reviewer 之前没深入看的区域（因为前几轮太多明显问题遮蔽了深层问题）。Rule #67 管初始提交质量（E2E trace），rule #65 管 rework 速度（24h 响应），但没有规则管 **rework 本身的质量**——逐条 fix comment 是 O(n) 轮次的做法，holistic re-review 是 O(1) 的做法。原则：**当同一 PR 累积 ≥2 轮 CHANGES_REQUESTED 且每轮都有新 Critical → 停止逐条 patch，做一次完整的 fresh-context diff review 再 push**。流程：(1) 收到第 2+ 轮 Critical feedback 后，先 fix 所有被点名的 issue (2) fix 完后**不要立刻 push**——先 `git diff main...HEAD` 看完整 PR diff (3) 以 reviewer 视角重新审查：「如果我是第一次看这个 diff，哪些地方会让我提 Critical？」 (4) 特别检查：(a) fix 新增的代码是否与其他部分 consistent（命名、错误处理、test 覆盖）(b) fix 是否遗漏了 reviewer 暗示但没明说的 pattern（如 reviewer 指出 path A 缺 X → 你只加了 A 的 X，但 path B/C 也缺同类东西）(c) fix 引入的新函数/参数是否需要自己的测试 (5) 如果 fresh-context review 发现 >2 个新问题 → 修完再 push，不要分批。判断方法：**如果你的 fix commit 只改了 reviewer 列出的精确行号/函数 → 你在做最小修改而不是理解 reviewer 的 intent**。reviewer 说「path A 缺 X」的 intent 可能是「所有 path 都应该有 X」——举一反三比逐条应对省轮次。推广：review roundtrip 的隐性成本不只是 calendar time——每多一轮，reviewer patience 下降、competitor window 打开、PR 复杂度上升（patch-on-patch 难读）。3 轮 CHANGES_REQUESTED 已经是 reviewer goodwill 的极限——第 4 轮很可能直接 close。与 rule #67（E2E trace pre-submit）互补：#67 防第 1 轮 Critical，本条防第 3+ 轮。与 rule #65（address feedback within 24h）互补：#65 管速度，本条管质量——快但粗糙的 fix 会制造更多轮次，抵消速度优势
